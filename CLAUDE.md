# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

`AGENTS.md` at the repo root is a symlink to this file so Codex and other agents read the same guidance. Edit this file only. Keep the **Current state** section at the bottom true; update it whenever something lands or changes, with the date.

## Product source of truth

`docs/PRD.md` is the source of truth for product scope, user journeys, team ownership, shared interfaces, safety rules, and acceptance criteria. This file remains the source of truth for repository conventions and implementation guidance. If the two conflict on product behavior, follow the PRD.

## What this repo is

**Thesis.fun** for the Thetanuts hackathon: a social options-trading app in the shape of fomo and pump.fun. A **trade** is a real options position on Thetanuts OptionBook, filled from the user's own wallet on **Base mainnet**, with its own page (`/p/<id>`) and live P&L card. A **post** ("thesis") is text like an X post; it may tag a market, and if its text links a trade (`/p/<uuid>`) the link unfurls into a clickable trade card, X-style. Likes, comments and follows on every post. Every position and result is verifiable onchain; losing theses cannot be deleted.

Owner decisions (all 2026-09-05, verbatim where it matters):
- **Every market Thetanuts has liquidity for.** Derive assets, strikes, expiries live from OptionBook orders. Never hardcode an asset list.
- Base mainnet only (chainId 8453).
- **A trade is just a trade; a post is just a post (14:2x):** "trade is just trade. post(thesis) is it's own thing. doesn't have to be tied. but when a user post it's trade (could be a link to his own live pnl card) in the trade then we render a nice card. u know how like if i share my own posts' link in x, x will render the posts' as a card? and not just the link? and it's clickable cuz issa link, then bring me to that card kinda thing?" Positions are independent rows (`role = 'standalone'`, `thesis_id` null; migration 0007); the market ticket never creates a post; `theses.creator_position_id` stays for the verified badge when a post links the author's own confirmed position — but the frozen 0002 trigger requires `role = 'creator'`, so a standalone trade can NEVER be linked that way today (owner schema decision pending).
- **A thesis is a post (earlier the same day):** "thesis doesn't really need user to actually put a trade first … a pure text opinion is fine also", "allowing users to like that particular thesis also". Text required; market tag optional; structure optional.
- **Trading lives on the per-asset market page** (from the fomo demo): live structures from the book, Bull/Bear ticket, no trade buttons inside posts. **No price chart anywhere (14:1x):** Thetanuts exposes only the live spot price, no history; "remove the chart then if this the case … think for the users man". After a fill, a dialog shows the P&L share card with "Copy link" and "Write a post about it" (fomo reference `.demo/fomo-share-card.png`).
- **Both sides ("do both, go"):** taker-BUY (pay premium; loss bounded) and taker-SELL (lock collateral; receive premium − fee; loss up to the collateral), each with honest sizing and copy. Everything a decoded fill has not proven stays gated in code (`VERIFIED_SELL_PAIRS`, `buyContractSizeDecimals`).
- **Creator payouts are FUTURE ROADMAP** ("we'll put creator fees as future roadmap, but we'll do callouts anyways for now. so we just do our own earning first"). Keep the attribution data; no ledger or payout UI. Roadmap line: "our platform earns through trading fees" = the referrer share below.
- **AI track:** the teammate's approval-gated trading agent stays as built ("keep how they made their ai stuff also. is fine"); `docs/PRD.md` v2.0 stands for it incl. §10.2 limits; rewired onto our packages (`packages/thetanuts` for orders/quotes/calldata, `packages/db` for thesis context). AI never signs by default; the wallet approves every transaction.

- **Owner ruling 2026-09-05 20:xx, REVISED 21:xx — autonomous trading is a STRETCH, not shipping.** The owner first put autonomous signing in scope, then on seeing the build cost against the deadline ruled: "lock in what's working, autonomy is stretch." So **what ships is unchanged: the AI never signs, the wallet approves every transaction.** `docs/PRD.md` §10.1/§10.2 stand as written and need no edit. Nothing in the shipping path may assume otherwise, and `hookup.test.ts`'s assertions that `execute.ts` contains no `sendTransaction`/`privateKey`/`walletClient` stay as the codified form of that rule.
- **Autonomous groundwork is parked on `origin/agent-auto-r1`, unmerged and not in main's migration chain.** It holds `agent_hedge_rules` (migration 0009, applied locally only) and a pure, 26-test rule evaluator (`apps/web/src/lib/agent/hedge.ts`). Deliberately not merged: an unused table in the production chain is a liability during review with no benefit. Findings that make it resumable are in `docs/HANDOVER.md` §9 — read that before re-researching, the mechanism is settled.
- **Constraint measured 2026-09-05, not negotiable by design choice:** `fillOrder(order, signature, referrer)` takes **no recipient** (`OPTION_BOOK_ABI`, `dist/index.js`); it returns `optionAddress` and the position goes to `msg.sender`. So an agent signing from its own EOA would own the user's option. Autonomous mode must therefore execute **as the user's account** (smart account / sub-account / session key), never as a separate agent wallet holding the position. Any design that has the agent's own address call `fillOrder` is wrong and must be rejected in review.
- **No Privy.** Wallet address is the identity (wagmi + viem, Coinbase Smart Wallet included). **Connecting a wallet creates the user profile** (create-or-fetch the `users` row keyed by the lowercase address, idempotent) — built.
- **Revenue = Thetanuts referrer fees** (docs "Referrer Fees"): every quote and fill carries `THESIS_REFERRER` (`packages/env` default = the owner's wallet `0xd5E66B6d957C2d5e6C8c167707a49a029D1247dd`, an EOA on Base); protocol fee `min(0.06% notional, 12.5% premium)` — BOTH branches fire on Base (see Thetanuts); our share `fee × splitBps / 10000` accrues per collateral token and is self-claimed with `claimFees(token)` from that wallet (needs gas). **The OptionBook owner must whitelist the address and set the split** (`setReferrerFeeSplit` is owner-only; unwhitelisted = 0, measured: `referrerFeeSplitBps` 0). Memo: `.research/thetanuts/monetisation-memo.md`.
- **Product numbers and copy are the owner's**: presets, trending/ending/leaderboard rules, content limits, session length, slug/handle bounds, fees, slippage, gas headroom. Tag `TODO-OWNER` in code and UI. Never invent a value.

Team: owner + Claude on product, UI and Thetanuts logic. Teammate on the AI track (feature branches; their push rule is not a merge approval).

Shared contract: `docs/PRD.md` v2.0 §10.3 defines `ThesisAiContext`; `packages/db/src/ai-context.ts` implements it exactly. Neither side changes it without telling the other and updating the PRD (§15).

## Design direction (fomo's feel; owner 2026-09-05 14:3x)

- **Owner (verbatim): "is it me or the ui/ux kinda suck ass again? idk. i still like fomo's feel. it's more classy" → "go with fomo's feel, redo the mockup now in parallel".** The earlier stadium/dark/gold design (Bricolage + Archivo + JetBrains Mono, gold `#F5C542`, icon rail, ticker tape) is gone.
- **Spec = `docs/mockups/thesis-fun-mockup.html`** (self-contained, 7 views + the post-fill dialog + a mobile feed; `docs/mockups/README.md` states the rules; screenshots in `.research/thetanuts/design-r1-shots/`). Design changes go through the mockup first (pixels before prose), then code.
- Tokens on `:root` (declared UNLAYERED in `apps/web/src/index.css`, because `packages/ui` declares shadcn's `:root` unlayered and unlayered beats every `@layer` — measured 2026-09-05 16:2x, accent had rendered grey): `--bg #0b0b10`, `--surface #14141b`, `--surface2 #1b1b24`, `--line #25252f`, `--text #f2f2f5`, `--muted #8d8d9c`, ONE accent `--accent #6f5cff` (+ `--accent-lift #a99bff` for text on tint), `--gain #22c55e`, `--loss #f4634f`.
- **Rules:** one accent, used only on primary buttons, the active tab underline, the selected side, the share-card frame, the "Open" chip and focus rings. Colour only on money (never bars, labels, names); the percent beside a P&L is neutral with a coloured arrow. `Manrope` only, `tabular-nums` on numbers; no mono, no display face. Radius by role (frame 24 · card 18 · panel/field 14–16 · row 12 · chip 999). Hairlines, not shadows (the dialog is the one blurred shadow). Base 14px, post body 16px, hero P&L 48px.
- **Chrome:** slim top bar (logo, centred search, wallet chip), horizontal nav `Feed · Markets · Leaderboard · Portfolio` with an accent underline, a persistent LEFT feed rail on every page except the feed, a RIGHT panel (ticket / positions / top traders), avatars everywhere. Posts look like a person said something. The share card (owner + status chip + date, instrument, huge signed P&L + percent, three stat tiles) is the position-page hero, the post-fill dialog and the trade card inside posts. No charts. Mobile (≤900px) hides the LEFT rail; below 1181px the right column STACKS into the main column (ticket first under the market header, other panels after the content, left rail last) — the mockup's own CSS hid that column, the owner delegated the call on 2026-09-05 19:3x and Claude decided to stack; a bottom-sheet ticket stays `TODO-OWNER`. The top-bar search is real (markets + people, ⌘K).
- The TradingView chart library was removed with the chart (`lightweight-charts` uninstalled; the owner's "keep the logo" ruling is moot).

## Thetanuts integration

- SDK: `@thetanuts-finance/thetanuts-client@0.3.0` (ethers v6 + viem), installed in `packages/thetanuts`. Docs: https://docs.thetanuts.finance/sdk (append `.md` to a page URL; full export at `/llms-full.txt`).
- **Read `docs/thetanuts-sdk-research.md` before touching the SDK.** It is a 999-line research report written by a codex sol and verified by Claude against the SDK bytes (14 load-bearing citations re-read). Its prompt is beside it.
- Verified facts that shape the code:
  - `encodeFillOrder` and `encodeApprove` return `{to, data}` calldata; wagmi/viem send them. No ethers signer needed for the app.
  - `client.api.fetchOrders()` returns every live maker order; assets come from `buildPriceFeedSymbolMap(8453)` (eight feeds configured: ETH, BTC, SOL, DOGE, XRP, BNB, PAXG, AVAX).
  - In a browser the client constructor throws unless `keyStorageProvider` is passed; use `MemoryStorageProvider`. RFQ is never used.
  - **Raw `isLong` is the MAKER's LONG flag — CORRECTED 2026-09-05 14:4x from chain bytes (`.research/thetanuts/finding-taker-side-inverted.md`): `isLong: true` → maker is the buyer → the TAKER SELLS and posts collateral; `isLong: false` → maker sells → the TAKER BUYS and pays premium.** The SDK comment at `normalizeOdetteOrder` ("isLong=true means maker sells, so taker buys") and its `isBuyer = !isLong` are WRONG; `packages/thetanuts/src/side.ts` was inverted until core round 9. Proof: tx `0x9c4bb1…` (`isLong=false`, taker = `OrderFilled.buyer`, paid 999998 USDC premium) and `0xdf3323…` (`isLong=true`, taker = `seller`, posted 22,000,000 aBasUSDC). Live book 2026-09-05 01:47 UTC (337 orders), corrected: isLong=true 125 = taker-SELL (aBasUSDC/aBasWETH/cbBTC collateral), isLong=false 212 = taker-BUY (182 in USDC, 30 in aBasUSDC). The teammate's agent "tradeable set" (`isLong === false` + USDC, "long option … most that can be lost is the premium") was RIGHT; Claude's `finding-isLong-side.md` is retracted. Every earlier statement of the opposite in this file is superseded by this bullet.
  - The budget passed to preview/fill is the taker's **premium spend** in collateral base units, not contracts. Contracts are capped at the maker's remaining size. Recompute premium as `numContracts * pricePerContract / 1e8`; never trust `preview.totalCollateral`.
  - Preview returns no max loss or max payout. Settlement is automatic on r12; `client.option.payout()` throws.
  - Do not use: `filterOrders` (drops `rawApiData`), `client.events.getOrderFillEvents` (stale event layout), `mmPricing` beyond ETH/BTC, `swapAndFillOrder`, RFQ, MCP.
- **Fill debits VERIFIED from decoded production fills 2026-09-05** (`.research/thetanuts/finding-fill-debits.md`, tx hashes inside): taker-BUY pays premium = numContracts × price / 1e8 in the collateral token (fee 12.5% of premium carved out of what the maker receives; maker posts strike × contracts or spread width × contracts); taker-SELL (put) posts collateral = strike × numContracts / 1e8 in the collateral token and receives premium − fee. `numContracts` is in 1e6 units for USDC/aBasUSDC orders (10000 = 0.01). Sell-side CALL collateral (SDK doc: 1:1 underlying) still needs a decoded example.
- **Fee: BOTH branches fire on Base.** The second decoded taker-sell (`0x3e7417c5…cff04`, core round 9) paid fee 737 on premium 9009 (8.2%, not 12.5%), so `feeEstimate` is an UPPER BOUND and `scripts/tiny-fill.ts`'s exact fee comparison would report "Receipt mismatch" after a good fill (owner call: loosen or keep strict). What "notional" means on chain is still unmeasured.
- **UNVERIFIED until tested with small real money**: contract-size units for non-USDC-family collateral (cbBTC, aBasWETH — 62 of ~125 live taker-buy orders stay gated); the taker-sell debit for anything other than PHYSICAL_PUT + aBasUSDC (calls, spreads, RANGER as seller); capped-budget rounding on chain; settlement timing and indexer P&L accounting. Code that touches these says so in its doc comment and stays gated.
- `@thetanuts-finance/mcp` was audited clean but is for AI chat clients; the owner ruled it out for the app.
- **Teammate-measured on Base mainnet 2026-09-05 (`docs/HANDOVER.md` §5; NOT re-verified by Claude):** maker signatures expire in 59–113 s and are re-signed about every 60 s, so approve collateral first, then re-fetch and build calldata within ~30 s; the SDK WebSocket host does not resolve (poll 20–30 s); `availableAmount` is the maker's collateral budget, not contracts; the book carries binaries (e.g. "ETH 2460 Up 1D"); tradeable set is USDC-collateral orders with `isLong: false` (~200 of 367); position read-back after a fill needs `api.triggerIndexerUpdate()` and polling; the seven `PHYSICAL_*` multi-leg implementations are not deployed on Base; RFQ has had no offers since 2026-08-20. Treat each as a hypothesis until our own tiny fills confirm it.

## Build status (every step below is CODED and merged on main; the one-shot review is what remains)

Owner's order was UI first, then core, then DB and socials, then wire, then polish. As of 2026-09-05 16:3x:
1. **Core trade logic** `packages/thetanuts`: buy + sell (rounds 1–9). Taker side corrected from chain bytes in round 9. 50 tests.
2. **DB** `packages/db`: migrations `0000`–`0007` (post model, likes, slugs/handles, follow activity, standalone positions), deferred creator-invariant triggers, migrate fence. 348 live tests.
3. **Wallet + fills**: sign-in with wallet (EIP-4361-shaped, single-use nonce, HMAC session), profile on connect, market page on the live book, ticket → quote → approve/simulate/fill → `recordTrade` from chain bytes (standalone positions; participant via `?thesis=`), post-fill share dialog, `/p/[id]` with the P&L card and share image.
4. **Socials**: posts (`publishPost`, `/p/` link unfurl → trade card), likes, comments, follows (+ activity), feed tabs, trending/ending/settled, leaderboard, profile editing (handle/name/bio). Every ranking/limit is `TODO-OWNER`.
5. **Polish**: Open Graph cards on `/t/` and `/p/`, `docs/DEPLOY.md`, `bun run verify`, env parity test, README.
6. **UI rebuilt on the fomo mockup** (three lanes merged; one fold round in flight).
The one-shot review RAN (all four lanes RED, see Current state); the fold of its findings is in progress (Writers B/C/D merged and pushed, Writer A landing). Still not done: the confirming review round (GREEN required before anything reaches production), the production migration, Vercel, the owner's tiny real fill.

Parallel track (teammate): AI track per `docs/PRD.md` v2.0 (`/agent`, `apps/web/src/lib/agent/*`, `packages/db/src/schema/agent.ts`, migration `0000_agent_tables`); their `origin/agent-exec-r1` (approval-gated execution on our `buildFillTransactions`) is being merged by fold Writer A (owner 18:2x "go on all"), with the money-path rules applied and a Lane C confirming pass to follow.

## How work is done here (owner rules, verbatim where it matters)

- **OWNER 2026-09-06 01:21 (verbatim, supersedes every writer-tier line below): "spin more opus. here's what we going to do instead. don't use astra low for work anymore. only astra medium for reviews and fixes until green. but fixes is done by opus. the other way basically. but review only start when coding is fully done."** Writers and fixers = Opus (several in parallel, own worktrees, Claude verifies each). Reviews = codex `gpt-6-astra` MEDIUM + Claude hands-on, started only when ALL coding is done; findings folded by Opus; repeat until GREEN. **Owner (same session, verbatim): "make sure to not trust yourself, don't trust opus and don't trust astra" and "make sure opus and astra themselves also don't trust themselves" — every writer and reviewer brief carries the SELF-DISTRUST BLOCK: measure every claim twice by different means; every fix ships with a mutant that goes RED; your own earlier conclusions in the same run are untrusted until re-measured; unverifiable claims are written NOT VERIFIED, never asserted. Claude re-measures everything on top.**

- **"NEVER TRUST YOURSELF, DON'T FUCKED UP."** Every claim, number, name and file path is verified at the source before it is used or said. A sol's output and Claude's own output are invalid until re-measured. Load the `verify-first` skill before claims and the `fable-method` skill before hard tasks.
- **Zero confidence in everyone, including yourself** (owner, verbatim: "none of yall should trust yourselves or the other. zero confidence in everyone's responses and findings"). Every brief carries this clause; every claim in a report is pasted measurement or it is not a claim; the orchestrator re-measures every headline number and every finding before folding or relaying it.
- **CODE FIRST, ONE-SHOT REVIEW (owner 2026-09-05 13:2x, verbatim: "let's skip all reviews. code ito ut first. then on shot review. keep in track what to review can already").** Refined by the owner minutes later (verbatim): "one opus high reasoning one astra low reasoning to code everything out first. then due to not trusting them, we review and verify. but don't trust yourself too later btw / also the reviews should do all under one astra medium and yourself, try to one shot the review also so it takes lesser rounds to reach where we want. i'm not asking we should lower our work quality btw". So: writers = ONE Opus agent at HIGH reasoning + ONE codex `gpt-6-astra` at LOW, in parallel, own worktrees; the review = the USUAL two-leg review (Astra MEDIUM + Claude hands-on, adversarial, digest-pinned, fold until GREEN), run ONCE after all the code is finished (owner 13:4x, verbatim: "the one shot review is meant for you to complete all the code then do the usual 2 leg review is what i meant. except we finish the work first then only review instead of plan -> code -> review -> plan fix -> code -> repeat until green which takes a lot of time"); Claude's own verification is re-measured in that pass too. No per-round adversarial reviews until the owner calls the pass. Claude still verifies every writer's claims (typecheck, tests, build, spot-checks at the bytes) before committing and merging — that is verification, not review. Everything merged unreviewed, and every carried finding, is logged in `.research/thetanuts/review-ledger.md` with what the one-shot review must attack. Nothing reaches production before that review is GREEN — owner 13:4x, verbatim: "the one shot review goes before we push for actual db migration on prod's db." Order: code → Claude verification → one-shot review GREEN → production `drizzle-kit migrate` (never `push`) → Vercel → real-money fills. The paragraph below describes the review shape when it runs.
- **Writer and reviewers are different agents.** Owner rule 2026-09-05 ("all towards gpt astra low effort and we review that"; earlier "don't use opus to work things not related to ui"): code is written by Opus workers (Agent tool, own worktree, fable-method first) AND codex `gpt-6-astra` at low reasoning (`codex exec -s workspace-write -m gpt-6-astra -c model_reasoning_effort="low"`; Astra needs codex CLI ≥ 0.153, a Homebrew cask, `brew upgrade --cask codex`), workload balanced between the two (owner 11:20 +08, superseding the earlier "no Opus for non-UI"), always from a pinned brief carrying the no-product-decisions block. Review is two legs: Claude (hands-on, adversarial, not the author) and codex `gpt-6-astra` at **medium** reasoning — **(codex ran out of credits 2026-09-05 12:12–13:15 +08; during such an outage the second leg is a fresh Opus adversarial agent, decided by Claude and flagged to the owner)** — (owner 2026-09-05: "medium for reviews should be enough, low for work") with a digest-pinned diff and a mandatory `Reviewed <path> SHA-256 <digest>` opener. Fold until both are GREEN. The codex sandbox has no network and no database and cannot write a worktree's git lock (it lives under the main repo's `.git`), so writers leave the tree uncommitted; Claude verifies, runs live tests and builds, and commits. Briefs and transcripts live in gitignored `.research/`.
- Parallel writers get separate git worktrees. Never two agents in one mutable tree. **Reviewers too: a detached worktree pinned at the reviewed commit** — Claude keeps committing state to the main checkout, which broke a review's HEAD pin on 2026-09-05.
- **Wide fan-out (owner 2026-09-05: "do everything at once so we spend lesser rounds … instead of brick by bricks")**: launch every independent piece in parallel with complete briefs (all known findings folded up front, completeness checklist), review with all lenses in one round, fold until GREEN.
- **Bun only.** Never npm, npx, yarn, pnpm. Registry lookups via `bun pm view`. shadcn via `bunx shadcn@latest add <name> -c packages/ui`.
- Never push without an explicit owner command. Never `--no-verify`. Local commits are fine.
- **Pull from GitHub whenever the team has pushed** (owner rule 2026-09-05): `git fetch origin` at the start of every work block and before every commit or worker launch; merge `origin/main` if it is ahead; re-read changed guidance (`CLAUDE.md`, `docs/PRD.md`) before acting; pin worker base commits to hashes on the merged main.
- The repo will be public: no credential-shaped values in tracked files. Env files: `apps/web/.env.local` (real values, gitignored) overrides `apps/web/.env` (gitignored; Claude's local `DATABASE_URL`, the production Supabase URL and password); `apps/web/.env.example` is the only env file in git and never holds a real value. `packages/env/src/server.ts` now also requires `OPENROUTER_API_KEY` (teammate's agent); local runs need a placeholder value in `.env`.
- Teammate's rule (their CLAUDE.md, 2026-09-05): they push validated checkpoints to feature branches under a standing approval they record from the owner; that approval is not approval to merge to main. Claude's own push rule above is unchanged.

## Commands

Package manager is **bun** (`packageManager` pin in root `package.json`). Turborepo drives tasks; `-F <pkg>` filters.

```bash
bun install                 # workspace install (run from repo root)
bun run dev                 # all apps; web is http://localhost:3001
bun run dev:web             # web only
bun run build               # turbo build
bun run check-types         # tsc --noEmit across packages
bun run verify              # scripts/verify.ts: typecheck + every suite + ONE db-mode next build (--offline skips live DB suites and the build)
cd packages/thetanuts && bun test          # offline unit tests for the trade logic
cd apps/web && DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/<migrated-throwaway> bun test   # web suite incl. integration files (they skip without DATABASE_URL)
# Smoke a PRODUCTION build only in DB mode against a migrated throwaway (next start refuses mock fixtures by design):
# cd apps/web && DATA_SOURCE=db DATABASE_URL=... bunx next start -p 3124
cd packages/thetanuts && bunx tsc --noEmit

# Database (Drizzle + Postgres, run from root)
bun run db:push             # LOCAL THROWAWAY DATABASES ONLY - see warning below
bun run db:generate         # write SQL migrations to packages/db/src/migrations
bun run db:migrate          # apply migrations
bun run db:studio           # Drizzle Studio

# Local Postgres via Supabase CLI (Docker), config in packages/db/supabase/
# Only the db (and its kong gateway) are enabled; auth, storage, studio, realtime,
# inbucket, edge runtime and analytics are switched off in config.toml on purpose.
cd packages/db && supabase start     # DB on postgresql://postgres:postgres@127.0.0.1:54322/postgres
cd packages/db && supabase status
cd packages/db && supabase stop

# The turbo db:* wrappers are marked interactive and fail without a TTY (agents, CI).
# Run drizzle-kit directly instead. No --env-file needed: see Env loading below.
cd packages/db && bunx drizzle-kit generate --name <change>   # write the migration
cd packages/db && bunx drizzle-kit migrate                    # apply it

# NEVER run `drizzle-kit push` against the shared Supabase project. push reshapes
# the database to match the schema of whoever runs it, so running it from a tree
# that lacks the other developer's tables can DROP those tables. Migrations are
# additive and reviewable; push is for a local throwaway database only.

# Vercel
bun run deploy:setup        # vercel link (once)
bun run env:preview | env:production   # push apps/web/.env to Vercel (scripts/sync-vercel-env.ts reads .env only, NOT .env.local)
bun run deploy | deploy:prod | deploy:check
```

No lint setup yet; `turbo.json` declares a `lint` task nobody implements.

## Architecture

Turborepo monorepo, all TypeScript, ESM. Workspaces: `apps/*` and `packages/*`. Shared dependency versions live in the root `package.json` `catalog` and are referenced as `"catalog:"`.

```
apps/web             Next.js 16 app (App Router, React 19, React Compiler on, typedRoutes on)
packages/thetanuts   @nuts/thetanuts: framework-agnostic trade logic on the Thetanuts SDK (+ bun tests)
packages/db          Drizzle ORM + node-postgres. Schema in src/schema, migrations in src/migrations
packages/env         Validated env via t3-env. `@nuts/env/server` (DATABASE_URL, DIRECT_DATABASE_URL?, SESSION_SECRET (≥32; required in production), DATA_SOURCE (mock|db; production — including `next build` — refuses mock; mock is `next dev` only), NODE_ENV, OPENROUTER_API_KEY, AGENT_MODEL, AGENT_GATE_MODEL, BASE_RPC_URL, THESIS_REFERRER, THETANUTS_ORDERS_URL), `@nuts/env/web`, `@nuts/env/load` (repo-relative .env.local/.env loader; note: it also fills values into test/probe processes, so an "unset" probe must set the variable to an empty string)
packages/ui          Shared shadcn/ui components on @base-ui/react + Tailwind v4. Exports globals.css, components/*, lib/*, hooks/*
packages/config      Shared tsconfig.base.json (strict, noUncheckedIndexedAccess, noUnused*)
docs/                PRD.md (v2.0), DEPLOY.md (runbook), HANDOVER.md (teammate), thetanuts-sdk-research.md (+ dated corrections), mockups/ (the spec + README)
scripts/             verify.ts (bun run verify), sync-vercel-env.ts (env:preview / env:production; reads .env only)
.research/, .demo/   Gitignored: Astra/sol briefs, transcripts, review diffs, findings; reference videos and screenshots
```

How the pieces connect:
- **Env files**: `apps/web/.env.local` holds real values and is gitignored; `apps/web/.env.example` is committed and documents every variable. `.env.local` overrides `.env`, matching Next.js precedence, and the real process environment beats both so Vercel and CI are unaffected. Copy to start: `cp apps/web/.env.example apps/web/.env.local`.
- **Env loading**: `@nuts/env/load` resolves the files relative to the repo, not the current working directory, and every entry point imports it. A script run from `packages/db` finds the same values the web app sees, so `bun --env-file=...` is no longer needed. Required today: `DATABASE_URL` and `OPENROUTER_API_KEY`.
- **Two database URLs.** `DATABASE_URL` is Supabase's transaction pooler (port 6543) and is what the app uses: Vercel functions open many short-lived connections and would exhaust a direct connection. `DIRECT_DATABASE_URL` is the direct connection (port 5432), used only by drizzle-kit because schema changes cannot run through the pooler. On a local database one string does both jobs and `DIRECT_DATABASE_URL` stays empty.
- **DB access**: import `db` from `@nuts/db`. It is created eagerly from `env.DATABASE_URL`, so importing `@nuts/db` in a client component will fail. Keep it in server components, route handlers, and server actions.
- **Env validation** runs at import time. `next.config.ts` imports `@nuts/env/web` so bad client env fails the build. `SKIP_ENV_VALIDATION=1` bypasses server validation.
- **Bun installs into an isolated store**: packages resolve through `node_modules/.bun/<name>@<version>/node_modules/...` and per-package `node_modules` symlinks, not a flat root `node_modules/<name>`.
- **UI imports**: `import { Button } from "@nuts/ui/components/button"`. The web app's `index.css` imports `@nuts/ui/globals.css`; design tokens live in `packages/ui/src/styles/globals.css`.
- **Path aliases** in `apps/web`: `@/*` → `apps/web/src/*`, `@nuts/ui/*` → `packages/ui/src/*`.
- **Providers**: `apps/web/src/components/providers.tsx` wraps the app in the teammate's `WagmiProvider` (`apps/web/src/lib/wagmi.ts`: Base only, injected + Coinbase Smart Wallet, cookie storage) and `QueryClientProvider`, then the forced-dark next-themes `ThemeProvider` and the sonner `Toaster`.
- **Web layout** (`apps/web/src`): `app/` routes `/`, `/m/[asset]`, `/p/[id]` (+ OG images), `/t/[slug]` (+ OG images), `/u/[handle]`, `/new`, `/portfolio`, `/agent`, `api/agent/chat`; `lib/` = `auth` (sign-in, sessions), `data` (DB reads, mappers, `DATA_SOURCE` switch), `market` (live book, taker side, quotes), `trade` (prepare/record/ticket), `thesis` (links, publish, enrich), `position` (instrument, P&L derivation, view), `social`, `profile`, `thetanuts` + `agent` (the AI adapter), `display*.ts` + `view-data.ts` (view layer on typed mock data); `components/` = `shell` (top bar, nav, feed rail, page frame), `primitives`, `feed`, `market`, `position`, `thesis`, `creator`, `auth`, `agent`. Domain types in `types.ts`, view types in `lib/display-types.ts`. Three historical handoff notes live under `docs/handover/` (`social-REPORT.md`, `data-REPORT.md`, `profile-REPORT.md`).
- **Vercel**: `vercel.json` deploys `apps/web` as the single `web` service with a root-level `bun install`.

## Next.js version warning

`apps/web/AGENTS.md` is auto-generated by `next dev` and says this Next.js 16 differs from training data. Before writing Next.js code, read the relevant guide under `apps/web/node_modules/next/dist/docs/`. Do not delete that file; `next dev` re-creates it.

## Current state (keep this true; last updated 2026-09-06 01:25 +0800)

- **main = `86a00a0` on GitHub (pushed 21:14).** Contains: Writer A merged (`9c040e7`/`6f5fef0`, scratch probe dropped `76f191d`), **Writer E merged (`81f8825`: generated `thesis-NNNN` names, DiceBear avatars, vendored coin logos; conflicts in `wallet-bar.tsx`/`position-rows.tsx`/`composer-data.ts` resolved as unions by the Opus merge worker, report `.research/thetanuts/merge-E-report.md`) + `e9da941` (market ticket requotes on a structure change — was only on side/amount; `structureChanged` pure + tested)**, `.env.example` with the three developer switches, the team's `docs/HANDOVER.md` note and their CLAUDE.md ruling change (`57cc082`, read it: autonomous mode opt-in). Merge worker's measurements on `e9da941`: check-types 5/5, thetanuts 51, env 10, db 360, web offline 394/41 skip, web live 520/7 on a fresh 0008 database, `bun run verify` 8/8, db build; identity walk alone in an isolated browser context: a new wallet got `thesis-1057`/`thesis_1057`, DiceBear `<img>` on every `.av` at 1440 and 390 (`monogramsLeft: []`), coin logos on pills/rail/search, zero console errors/broken images/overflow (14 shots). Re-measured by Claude on `e9da941`: clean tree, parents `76f191d` + `0c24756` are ancestors, sweep for hand-rolled `span.av` empty, check-types 5/5, offline 435 ran / 0 fail / 41 skip; **Claude's live run on main (21:15, fresh 0008 database `claude_main`): web 520 pass / 7 skip / 0 fail, db 360/0, thetanuts 51/0, env 10/0** (env was 9/1 until `ad3463c`: the three developer switches in `.env.example` are now commented examples, because `packages/env/test/env-example.test.ts` requires the example's keys to equal the schema's exactly). GitHub main = `eec15e8` (21:33; includes the team's `b0fda40`: autonomous trading is a stretch, not shipping). Earlier history: Writer A merge details below; verification at `a3bedd2`/`47e5de7` (17:5x–18:5x): check-types 3/3, web offline 277/23, live 377, package suites thetanuts 50, DB 348, env 10 (16:2x).
- **UI fold round 1 MERGED (`ce558e2`, ledger item 28).** Opus HIGH writer, brief `.research/thetanuts/brief-ui-fold-r1.md` (25 items); shots `.research/thetanuts/ui-fold-shots/` (writer's `fold-*` + Claude's `claude-*`). One `PnlCard` builder for post cards, share card and fill dialog; `PageFrame` + `FeedRail` on every page; feed pills rank one post list; Follow with follow state; shared status vocabulary; mock `/p/` fixture carries an example order snapshot; 390 px nav and wallet-handle overflow fixed. Writer refuted items 3 and 25 with measurements; items 11 and 22 are owner calls (tiles are entry · max loss · max payout; the mockup hides the trade ticket ≤1180 px). Seen, not fixed: the "post this backs" panel on `/p/[id]` clips its last line.
- **Taker-side inversion FIXED on main (`a3bedd2`, ledger item 27).** `apps/web/src/lib/position/instrument.ts` had its own copy of the `isLong` mapping, inverted (`isLong ? "buy" : "sell"`) against the chain-verified rule; it now calls `measuredTakerSide()` from `lib/market/taker-side.ts`. Tests re-pinned; a mutant that re-inverts the line fails 5 tests; the mock `/p/` page now reads "Premium paid · Long the structure (bought)" for a bought put. Reviewers attack the fix and every consumer of `takerSide`.
- Lane A review DONE and FOLDED: Astra MEDIUM (pinned worktree @ `510226c`, `review-oneshot/astra-review-A-r2.out`) was RED — MAJOR: the production data-source guard's build-phase exemption let a prerendered mock page be served from cache with no runtime check (Claude's own code; agreed and removed — `next build` now REQUIRES `DATA_SOURCE=db`, measured); MINORs: `PGOPTIONS` bypassed the migrate fence (now refused), DB README stale (fixed). Confirming Astra pass runs with lanes B–D.
- **ONE-SHOT REVIEW IN PROGRESS at `c2d6044` (Astra MEDIUM legs in pinned worktrees `review-lane-{A,B,C,D}`; briefs `review-oneshot/brief-review-*-r*.md`; lane diffs + digests in `review-oneshot/review-lane-*.diff`; lanes cover 212 of the 215 files changed since `d4a4597`, the other 3 are CLAUDE.md/README/PRD):**
  - **Lane B (server, socials, identity, profile): Astra RED — 7 MAJOR, all CONFIRMED by Claude at the bytes** (`review-oneshot/claude-adjudication-B.md`): challenge burn DoS (`consumeChallenge` before the signature check); fixture Markets panel + composer pills in db mode; an `expired` thesis crashes the feed (two public-status lists: `PUBLIC_THESIS_STATUSES` vs `SOCIAL_PUBLIC_STATUSES`); leaderboard inner-joins `theses` → standalone trades excluded; null premiums counted as fills but not as unusable; position reads leak a draft's headline; untagged rail limit + copy. MINORs: uuid-shaped slug precedence, no session revocation (owner), wallet-switch identity, unbounded reads, profile policy items, unfenced test DB, `REPORT.md` under `src/`, `quote.integration.test.ts` needs network at top level.
  - **Lane C (trade, position, links, agent, 0007): Astra RED — 13 MAJOR, all CONFIRMED** (`claude-adjudication-C.md`): `contractsFrom` fails OPEN (accepts the ticket's count on mismatch/undecodable tx if premium matches; the event is not bound to the maker); pending row inserted before ownership check → anyone can reserve another wallet's tx hash; retries not bound to the original ticket, no conditional status transition; `sendTransactionAsync` without `account`/`chainId`, and preparation continues on the approval HASH; the fresh server quote is never shown before sending; `fillCard` post-commit read failure → the client re-sends a NEW fill; no expiry reconciliation (`markIndexed` does not exist) → P&L at spot forever; linked SELLER cards say "Premium paid" with the net premium received; copied links are ABSOLUTE and never unfurl (no origin passed; headline never scanned); db-mode market page falls back to fixtures on a feed error; cbBTC liquidity shown as "$1"; OG image lacks the basis line; market-rail copy promises the verified badge. MINORs: offline suite network gate, `feed_unusable` gaps, dialog clipboard fallback, fee sentence, OG font.
  - **Lane D (UI): Astra RED — 9 MAJOR (5 overlap B/C; new: market "Backed" filter always empty, list rows lose lifecycle, post→market links drop the thesis so the participant journey is unreachable, post OG image still gold + Inter font) — confirmed at the bytes. Lane A r3: RED on the fixture-markets major + 4 minors (verify script passes with skipped live suites; env package fails tsc; buy approval amount untested; DEPLOY.md stale).** Every finding is itemised for the team in `docs/OPEN-WORK.md` (owner 18:3x: "some work here my team continue better. list them all out … and push everything"). Claude legs C and D done (`claude-review-C.md`, `claude-review-D.md`): third fill `0x3e7417c5…` reproduces every record-path rule; dead CSS; headline inside a 38 px button clips.
  - **User-flow walkthrough (owner 18:0x: "have u made sure the userflows … nice and smooth?" → no; then "let these testing done by opus"):** Claude walked J1–J8 in db mode with a fake EIP-1193 wallet + local signer (`review-oneshot/userflow-journeys.md`, harness `fake-wallet.js` + `signer.ts`, shots `ui-fold-shots/uf-*`); an **Opus HIGH tester is RUNNING** (brief `brief-userflow-opus.md`; report → `review-oneshot/userflow-opus-report.md`; own DB `uf_opus`, ports 3133/3134/3198). Findings F1–F10: fixture "9 new theses" banner and fixture Markets panel in production (MAJOR); inert Share/Explain buttons; "—" as the name and 42-char address handles everywhere (also the page `<title>`); the "Latest theses" rail overflows into the main column on every page (owner screenshot 18:1x: "rmb to fix this"); signed-out comment box disabled with no hint; the Ranger C suffix survives on the LIVE market page (`live.ts:241` bypasses `strikeSide`); "24h —" dead cell; worst-value structure is the default; signed-in-but-disconnected → "Connect your wallet first" with no connect control anywhere (F9); no link to your own profile/editor from the chip or portfolio (F10); the copied absolute link does not unfurl and the URL leaks into the slug; the unfurled card and the position page tell different stories (BTC vs ETH put; "not available" vs −$1.00). Smooth: sign-in (SIWE message, profile row, no refresh), post, like, comment, follow (shared state), the approve prompt carries exactly the quoted premium.
  - **AI hookup (owner 18:2x "did u forget to hook up the ai part…" → "go on all"):** fold items — "Explain" opens `/agent` with the thesis preloaded (the agent already has a `getThesisContext` tool; the page takes no param today) and the agent's answer links the market page with the structure selected; **`origin/agent-exec-r1` (2 commits, 5 files, +560: approval-gated OptionBook execution on `buildFillTransactions`) joins main via the fold**: trial merge in `.claude/worktrees/agent-exec-trial` (`ab3bab0`) has zero conflicts, `tsc` clean, offline 277/23/0 — unreviewed; it gets a Lane C confirming pass.
- **Fixed on main after the review pins (owner 18:4x, screenshots: "fix this problem please and these too"):** `47e5de7` — unnamed people show a shortened address (never "—"), the printed handle is shortened when it is the address, the "Latest theses" rail name wraps (walkthrough F4/F5/F6). View layer only; tsc clean; offline 277/23/0; live 377/0 on a fresh 0007 database; measured in a db-mode production build: rail name right edge 289 px inside its card (312 px), byline/handle/creator card `0xb792…20e3`. Pending the confirming review like everything else.
- **Owner rules added 18:5x:** run every command in the background (results read from the task output on completion); Devfolio MCP server added at user scope and authenticated (`mcp__devfolio__*` tools) for the hackathon submission. Pushed to GitHub at the handover commit (owner: "push everything"; standing: "keep pushing whatever is done … once it's done and green").
- **Not bland (owner 19:5x, verbatim: "Generate random names for users if they dun wan put and allow edit later is fine also / Like thesis-1234 or something / Then the assets image go get from somewhere not malicious / We need it so our product dun show too bland"): Writer E (Astra LOW) DONE → commit `66702da` on branch `fold-identity` (base `3929410`), committed by Claude after re-measuring: tsc clean; offline 339 ran / 0 fail / 30 skip; live 413 pass / 7 skip / 0 fail on a fresh 0007 database (`claude_e`); every `<Avatar>` site passes `seed` or `asset`; no package change; 29 files, +269/−38.** Generated names (`display_name thesis-NNNN`, `handle thesis_NNNN`, editable later; savepoint per attempt, five retries, then null handle with the name kept), DiceBear `thumbs` avatars seeded by the wallet address (core 9.4.3 + collection 9.4.2, MIT; no network, no storage), vendored CC0 coin icons (`apps/web/public/asset-icons/`, eight Base feeds; `assetIconPath` is display-only). Report in its worktree `.research/thetanuts/fold-identity-report.md`. Opus browser tester on `66702da`: RED — names/avatars/logos/edit-persistence all OK, 0 console errors, but four hand-rolled `span.av` spots were missed (composer avatar + market pills, comment box, a nested avatar in position rows) → **round 2 DONE → `0c24756` on `fold-identity`** (Astra LOW; re-measured by Claude: sweep for hand-rolled `span.av` empty, tsc clean, offline 345/0/30, live 419/7/0 on a fresh 0007 database, db build, curl on the production build shows 6 logo pills + the person avatar in the composer and comment box); the "chip shows the address" finding is refuted by the mockup (owner item). `fold-identity` = `66702da` + `0c24756`; MERGED to main (`81f8825`, see the main bullet). Details `docs/OPEN-WORK.md` §8. Every value `TODO-OWNER`; plan in `docs/OPEN-WORK.md` §8. NOT yet merged: it shares 12 view files with Writer A, so it merges AFTER the A merge lands.
- **Owner 20:0x:** Devfolio: apply to BOTH Thetanuts tracks (recorded in `docs/DEVFOLIO-SUBMISSION.md`); writers are Opus + Astra LOW, Claude orchestrates and verifies; ONE review after all coding lands, fold until GREEN.
- **Handover:** `docs/OPEN-WORK.md` is the tracked, self-contained list of every open item (review findings B/C/D/A, walkthrough F1–F16, AI hookup, agent-exec merge, owner decisions, deploy order). Keep it true alongside this section.
- **main on GitHub = the merge of Writer B (`40ea809`), Writer D (mobile stacking) and Writer C (search) — pushed 19:5x (owner: "and push also"); Writer A (money path) still to land.**
- **THE FOLD (base = pushed main `c007018`):**
  - **Writer B (Astra LOW, branch `fold-ui`) DONE and VERIFIED by Claude → commit `40ea809`** (tree committed by Claude; the writer's report is in its transcript `fold-ui-astra.out`, tokens 188,374, and `.claude/worktrees/fold-ui/.research/thetanuts/fold-ui-report.md`). Re-measured: tsc clean; offline 283/30/0; live 383/7/0 on a fresh 0007 database; db-mode `next build`; `LIVE_BOOK_TESTS=1` quote suite 7/0 (356 live orders, 0 side disagreements); browser on its production build: feed Markets panel = 6 live assets with spot only, no fixture banner, Share/Explain real; market page without the 24h cell, Ranger without C, Backed tab; composer pills = the live set; one `main`. Delivered: live markets in db mode (B2/C10/D2), origin-aware links scanning the headline (C9/D3), token units (C11), OG palette + Manrope (C12/C-m4/D8), honest badge copy (C13/D9), `CopyLink` in the dialog, Backed filter via `withCards` (D4), `?thesis=` post→market links (D6), Share/Explain wired (D7), pills as buttons (D-m1), chart remnants + dead CSS gone (D-m3/D-m5, `.act`/`.av-30`/`.av-44` refuted as live), headline out of the button (D-m6), `strikeSide` on the live page (D-m7), B7 tags, REPORT.md → `docs/handover/`, `LIVE_BOOK_TESTS=1` gate (B-m8), F7/F9/F10/F11/F12/F14/F16. Caveats for the confirming round (`.research/thetanuts/fold-ui-adjudication.md`): OG fonts fetched from Google at request time (vendor locally — handed to Writer A); preview-deployment origin vs Host (accept either — handed to Writer A).
  - **Writer A (Opus HIGH, branch `fold-money`) DONE at `0b05587`** (report `.research/thetanuts/fold-money-report.md`; 61 files, +6028/−205 measured by Claude; base `c007018` confirmed as ancestor; tree clean). Writer's claims (NOT yet re-measured): `bun run verify` 8/8, check-types 5/5, db 360/0, thetanuts 51/0, env 10/0, web live 459/0, offline 329/0, db build, migration **0008** (partial unique on tx hash), C1–C8/C-m1/C-m5, B1/B3–B6/B-m1/B-m6, A-m1–A-m4, D5, F14/F16/F18/F19/F20/F23/F24/F29, `origin/agent-exec-r1` merged and rewired onto `prepareTradeFor`, AI hookup (`/agent?thesis=`, `/m/<asset>?thesis=`). STOPPED (files only on Writer B's branch): 9(a) OG fonts vendored locally, 9(b) site-origin dual-match. No browser pass on seeded data. Writer's own attack list in its report §5 (first: `claimPending`'s supersede path writes to a row the caller does not own). **Merge onto main (`56b7014`) dry-run by Claude: ONE conflict, `components/auth/wallet-bar.tsx` (B's Profile link/connectors vs A's F14 chain guard). An Opus merge worker (brief `brief-merge-A.md`, worktree `merge-trial-A`, launched 20:15) resolves it, applies 9(a)/9(b), re-measures every suite on a fresh 0008 throwaway, builds, walks the browser on seeded data, commits the merge; report `merge-A-report.md`; Claude re-measures before fast-forwarding main.**
  - **OpenRouter has no credits (Writer A, measured: "requires more credits… you requested up to 1200 tokens, but can only afford 1105")** — the agent's code path is proven but it cannot answer until the owner tops up the key. OWNER ITEM.
  - **Writer D (Astra LOW, branch `fold-mobile` from `40ea809`) DONE and VERIFIED → commit `2fb52a4`.** Owner 19:3x delegated the phone-ticket decision ("whatever makes sense … if can't decide then ask astra"); Claude decided: below 1181 px the right column STACKS into the main column (ticket first under the market header; other panels after the main content; the left rail last — Claude added `.col-left{order:9}` after measuring it pushing the header down 330 px at 901–1180 px); a bottom sheet stays `TODO-OWNER`. Writer used `display:contents` on the column wrappers + grid `order` (one DOM, one ticket). Re-measured by Claude: tsc clean; offline 285/30/0; db build; browser on the production build at 390/768/1000/1180/1181/1440 — Trade visible at every width, ticket directly under the header below 1181, desktop unchanged at 1181+, no horizontal overflow. Shot `ui-fold-shots/fold-mobile-ticket-390.png`. Resolves tester F13.
  - **Writer C (Astra LOW, branch `fold-search` from `40ea809`) DONE and VERIFIED → commit `5529a1f`, MERGED to main:** search box is real — `components/shell/search.tsx` + `lib/search/{actions,reads}.ts`, top-bar wired, ⌘K; writer claims tsc clean, offline 292/30/0; its `TODO-OWNER`s: 100-char cap, 10 people, 250 ms debounce, wording, ordering, arrows wrap, hidden ≤900 px. Re-measured by Claude: tsc clean; offline 292/30/0; live `searchPeople` on a real 0007 database with a seeded user (handle prefix, name substring, wallet prefix hit; `%`/`_` match nothing); db build; browser on the production build: ⌘K focuses, "bt" → `/m/btc`, "sear" and "0xabcd12" → `/u/searchme`, nonsense → the no-match line, Enter opens the first result (shot `ui-fold-shots/fold-search-people-1440.png`). Resolves tester F17. Launched 19:3x — owner: "build the search then for markets and/or find ppl/wallets": real `<input>` in the top bar, ⌘K focus, listbox of Markets (live book) + People (handle prefix / name substring / address prefix via a new `lib/search/reads.ts`, parameterised, `%`/`_` escaped), server action `searchAll`, Enter opens the first result; limits/debounce/copy `TODO-OWNER`. Brief `.research/thetanuts/brief-fold-search.md`, wrapper `run-astra-fold-search.sh`, output `fold-search-astra.out`.
  - **Opus user-flow tester DONE — RED: 2 BLOCKER, 8 MAJOR, 16 MINOR** (`review-oneshot/userflow-opus-report.md`; F1–F10 re-measured, F4/F5/F6 confirmed fixed by `47e5de7`; all dispositions in `docs/OPEN-WORK.md` §4c). New and confirmed at the bytes by Claude: F15 `/agent` dead (Writer A); F14 chain guard dead (`useChainId()` returns the config chain; Writer A); F13 **BLOCKER, OWNER DECISION**: nobody can trade below 1181 px (the mockup's CSS hides the right column; options: ticket into the main column on small screens, or a bottom sheet); F17 search box inert → owner ordered search (Writer C). Process rule from F28: a tester certifies a commit from a DETACHED worktree at that commit (main moved under it).
- **Owner 21:5x (standing): "Rmb to keep pushing github in case laptop no battery so my team can continue work" — push main after every landed step and push in-flight `fold-*` branches as they gain commits (fetch + merge `origin/main` first; never force). At 21:59 GitHub has main `5f278ae` and branches `fold-C-r2` (`90c686b`, Opus writer in progress), `fold-identity`, `fold-money`.**
- **Owner 21:2x (standing): "Rmv to keep migrating green changes to prod db" — once a change is GREEN, Claude migrates the production database (`DRIZZLE_ALLOW_REMOTE=1 DATABASE_URL=<session pooler> bunx drizzle-kit migrate` from `packages/db`, never `push`) without asking again, and reports what was applied. Production measured 21:16: 1 migration (`0000_agent_tables`), six `agent_*` tables; 0001–0008 apply after lane C is GREEN.**
- **DEVFOLIO SUBMITTED 22:50 +0800 (owner 22:4x: tagline "Say it then trade it. Trade real onchain options on base" → formatted "Say it. Trade it. Real onchain options on Base."; "Make the formatting nicer / Then submit"). Verified `publish`; details in `docs/DEVFOLIO-SUBMISSION.md`. Work continues after submission (owner: images not demo). **23:28: live site link added — the team deployed `https://nuts-web-kappa.vercel.app/` (200, renders); its database is production, which still holds only migration 0000 → the site cannot serve users/theses/positions until 0001–0008 are migrated after GREEN.** Two Opus agents (fold C r3, lane A review) died ~22:57 in a network outage (API ENOTFOUND); network back 23:27; fold C r3 RESUMED (7 commits on disk + D5 in progress).**
- **DEADLINE (owner 20:5x, MET): the Devfolio submission had to be in before 23:00 +0800 today. Owner: "we can keep working on it after submitting also since it asks for images and not demo" and "dun submit now let's keep working and polishing first" — so: polish + review now, screenshots from the merged tree on a clean database, submit near the deadline ONLY on the owner's "submit". Time-boxed review: lanes C (money path) and B (auth/identity) RUNNING since 21:00 at pin `e35eb43` (first launch 20:58 died in a minute — the generator wrote the old digest and wrong paths into the briefs; both reviewers correctly STOPPED on the pin mismatch; fixed, relaunched; digests C `d1c85c1f…` 6673 lines, B `545173af…` 2320, A `df67c29f…` 2820, D `240e1fa2…` 3041) (Astra MEDIUM, worktrees `review-confirm-C/B`, outputs `review-confirm/astra-confirm-{C,B}.out`); lanes A and D get Claude spot checks + the two Opus browser walks already done, not an Astra pass tonight. Live trading deploy tonight only if lane C is GREEN (Claude's recommendation; owner decides).**
- **Demo seed + Devfolio screenshots: an Opus worker (brief `.research/thetanuts/brief-demo-seed.md`, worktree `demo-seed`, database `demo_seed`, port 3171) seeds 5 people (3 named, 2 generated) + ~8 realistic posts + comments/likes through the app's own server functions, shoots `docs/screenshots/01…07` from a db-mode production build, then waits for the merged hash to re-shoot. Report `demo-seed-report.md`.**
- **Confirming review round PREPARED (20:5x), launches on the merged commit:** generator `.research/thetanuts/review-confirm/make-confirm-round.sh <full-hash>` pins per-lane cumulative diffs `c2d6044..<hash>` (A: packages + scripts + deploy docs; B: auth/data/social/profile/search/avatar/site-origin/og-fonts/auth+creator components; C: thetanuts/agent/api/market/trade/thesis/position + migrations; D: app routes, components, display, css, public, mockup), digests, detached worktrees `review-confirm-{A,B,C,D}` (with `.env` + `bun install`), briefs `brief-confirm-<L>.md` (= the one-shot brief's settled-facts header + `lane-<L>-section.md`: fold-verification lens over every OPEN-WORK item, then the full lens list) and wrappers `run-astra-confirm-<L>.sh` (Astra MEDIUM, read-only sandbox, max 2 at once). Second leg = Opus adversarial agents (money path C; socials/identity B+D) + an Opus tester re-walk from a detached worktree; Claude adjudicates every finding at the bytes; fold until GREEN.
- **Lane B confirming pass (Astra MEDIUM, pin `e35eb43`, `review-confirm/astra-confirm-B.out`, 21:18): RED — 2 MAJOR, 4 MINOR.** Claude at the bytes: (1) MAJOR TRUE — both test-database fences (`apps/web/test/setup.ts:40`, `packages/db/test/setup.ts:21`) check `new URL(raw).hostname` while `pg-connection-string` lets `?host=`/`hostaddr=` override the destination (probe: fence `null`, driver host `remote.example`); (2) MAJOR REFUTED — "generated names/avatars did not land": true at that pin, which predates the identity merge (`81f8825`); a brief error, the identity code needs its own pass at the new pin; (3) MINOR B-m3 wallet-switch identity still open (owner behaviour choice); (4) MINOR TRUE — `rankedTheses`/`leaderboard`/`listActivity` have no LIMIT and `discoverData()` reads follow state per creator twice; (5) MINOR TRUE — `next/og` fetches twemoji from jsDelivr when a headline has an emoji (owner choice: strip or accept); (6) MINOR TRUE — the disconnected wallet menu falls back to the fixture label `wallet.network`. B1–B6 folds confirmed landed by the reviewer with mutants. **Fold B r2 DONE and MERGED (`18e3dc3` → main `bb41126`, pushed in `eec15e8` 21:33):** both fences + `scripts/verify.ts` refuse `host`/`hostaddr`/`service`/`servicefile`/`connectionString` URL parameters (Claude's probe after: refusal string naming the parameter; `sslmode` and plain loopback still pass); `rankedTheses` limit 6 and `leaderboard` limit 7 (`TODO-OWNER`, mockup rows) + one batched follow-state query; wallet menu label from `config.chains[0].name`; `listActivity` bounding STOPPED (no owner number — owner item). Re-measured on the fold tree: tsc clean, offline 446/0/41, live web 531/7/0 + db 366/0 on a fresh 0008 database, env 10/0.
- **Lane C confirming pass (Astra MEDIUM, pin `e35eb43`, `review-confirm/astra-confirm-C.out`, 21:20): RED — 1 BLOCKER + 12 MAJOR.** Claude at the bytes on main: BLOCKER TRUE — `components/agent/agent-chat.tsx:153` expects `part.type === "tool-approval-request"` but `ai@7.0.92` emits `tool-<name>` with `state: "approval-requested"`, so Approve/Cancel never render (agent execution unusable); MAJOR TRUE — a failed recording lets the ticket send a SECOND fill (C6 incomplete); indirect reconciliation accepts the ticket's contract count on a premium match that is not injective (`record.ts` ~729); a vanished requested structure silently becomes the default (`live.ts:343`, PRD §8.4); expired positions still read "Open · syncing" in compact cards (C7 incomplete); agent ceiling checked on the first quote not the prepared economics + aBasUSDC accepted (PRD §10.2); agent C5 compares undisplayed values; agent retry uses the wrong token and dies on quote expiry; no server-side daily caps (PRD §10.2 10/50; `agent_usage` unused); `/agent?thesis=` never sends `thesisId`; Explain cannot read a text-only post; untagged `MIN_SIGNATURE_SECONDS = 25` / `.max(80)`. REFUTED at HEAD: "structure change leaves the old quote" — fixed by `e9da941` after the pin. Residuals: malformed chat bodies throw, agent market links are plain text, legacy "Premium paid" at `position/view.ts:383`, `font-mono`/amber in agent components, scope-gate `degraded` ignored. **Fold C r2 DONE and MERGED (`0302950` money path + `90c686b` agent → main `af3cd2f`, pushed 22:06; report `fold-C-r2-report.md`, probes `fold-C-r2-probes/`, shots `fold-C-r2-shots/`).** Writer's measurements: tsc, check-types 5/5, offline 447/49, live 579/8/0 fresh 0008, thetanuts 51, db 360, env 10, db build, `bun run verify` 8/8, `LIVE_BOOK_TESTS=1` 8/0 (360 structures, 0 side disagreements), browser pass alone (structure switch requotes; vanished `?structure=` → no ticket; `/agent?thesis=` renders Approve/Cancel with `thesisId`; OpenRouter 402 so the chat reply was stubbed with real SDK chunks). Re-measured by Claude: clean tree, `merge-tree` no conflicts, key bytes present (`approval-requested`, `FILL_QUANTITY_UNPROVEN`, `requestedStructureMissing`, 429 daily cap, `MIN_SIGNATURE_SECONDS = 30`), tsc clean, offline 496/0/49, live 579/8/0, thetanuts 51/0. Every reviewer item fixed with RED→GREEN probe + mutant, #9 refuted at HEAD, C8 legacy "Premium paid" in `backingCard` STOPPED (no writer for `theses.is_long`). **Writer's open items → owner/review:** a page reload loses the sent-fill hold (the `pending` row survives but nothing reads it → reload + Trade could send a second fill); `FILL_QUANTITY_UNPROVEN` refuses batched smart-wallet fills until a batch layout is measured; the scope gate's `degraded` now returns 503 (was fail-open). **Lane C SECOND pass RUNNING since 22:07 at pin `af3cd2f` (diff 8938 lines sha256 `8a8844e8…`; first pass kept as `astra-confirm-C-r1-pin-e35eb43.out`) and **lane D FIRST pass (22:25, `astra-confirm-D.out`): RED — 3 MAJOR:** D5 incomplete (list rows derive lifecycle from the persisted status while the position page resolves expiry from the instrument + clock: a `confirmed` row past expiry reads "Open · syncing +$612" in lists and "Settlement pending —" on its card; feed/market/portfolio rows show no status/basis; `map.ts:357` drops the instrument expiry), D-C1 the agent recording retry mishandles a rejected or failed transaction (`components/agent/trade-execution.tsx:165`, from fold C r2), D-C2 new agent copy untagged. Offline 460/0/49 at the pin. To be folded TOGETHER with lane C's second-pass findings in one writer round.** **Lane C SECOND pass (22:32, `astra-confirm-C.out`): RED — 9 MAJOR, all component-level (C1–C5/F14 folds confirmed with mutants):** (1) a side change during a pending preparation can display Sell and send Buy (`take-a-side.tsx:488` compares the quote captured at `sign` start; controls stay enabled); (2) a REMOUNT loses the sent-fill hold → another fill (the writer's own open item); (3)/(4) agent retry outside the try + no `failed` check (= D-C1); (5) approval-stage results bypass the agent limits and the approval is sent before the gated re-preparation (`limits.ts:49`); (6) missing-structure recovery buttons inert; (7) a text-post conversation cannot execute a later-selected trade; (8) agent fill calldata broadcast beyond PRD §14's 30 s (fetch age unchecked); (9) `fill_quantity_unproven` rows read "This transaction failed, so there is no position". **Fold C r3 RUNNING (Opus HIGH, worktree `fold-C-r3`, brief `brief-fold-C-r3.md`, launched 22:34; branch auto-pushed on each commit): all nine + D5 + D-C1 + D-C2; Claude's product call for (2): persist the sent fill in `sessionStorage` per wallet + restore on mount showing the existing "Record the fill" control + a server refusal while a young `pending` row exists (window `TODO-OWNER`).** Lane A pass (packages/env/deploy): first launch died at 22:46 with codex "Selected model is at capacity" after the pin check (`astra-confirm-A-capacity-fail-2246.out`); relaunched 22:47 and died the same way at 22:55 (`…-capacity-fail-2255.out`) → per the codex-outage rule the lane A leg became an OPUS adversarial reviewer (worktree `review-confirm-A` pinned at `af3cd2f`, report `review-confirm/opus-confirm-A.md`, died in the 22:57 outage after measuring, resumed, reported 23:3x): **RED on docs/scripts only — the code passed every attack** (migration 0008's partial index + CHECK behave in 6 raw-SQL cases; `0001`–`0008` commit in ONE transaction on a `0000`-only baseline = the production shape; `drizzle-kit check` + dry `generate` clean; all three fences + `scripts/verify.ts` fail closed incl. `?host=`; a 25-case fuzz of the fence predicate vs the installed `pg` driver found 0 holes; A-m3 approval mutant killed; `bun run verify` 8/8; suites 51/10/366/593-8-0). MAJORs: (1) `docs/DEPLOY.md` said count 8 and `0000`–`0007` — FIXED by Claude (23:33: 9, `0000`–`0008`); (2) `packages/db/README.md` never learned 0008 — FIXED by Claude; (3) `scripts/sync-vercel-env.ts` pushes every `.env` key with `vercel env add --force`, warning but not refusing local values, and would upload `PROD_*`/`SUPABASE_*` secrets — handed to an Opus writer (refuse, never upload); (4) `scripts/` is outside `check-types` (3 real diagnostics) — same writer. MINOR: `outputFileTracingIncludes` for the OG fonts is a no-op (fonts traced without it; comment unproven). Originally launched 22:34 at the same pin. Still NOT GREEN → no production migration yet.
- **Devfolio screenshots READY and committed (`docs/screenshots/01…07`, from main `86a00a0`, seeded clean database `demo_seed`, Opus worker report `.research/thetanuts/demo-seed-report.md`; seed script + keys gitignored under `.research/thetanuts/demo-seed/`; one REAL position replayed from Base tx `0x9c4bb1…`).** Claude looked at 01 and 02: names, faces, coin logos, readable theses, live BTC book + ticket. Nits seen (owner items unless trivial): rail shows "—" under a name and handles instead of names; "since Sep 26" reads as a day; the ticket sentence prints "249.999966 USDC" (raw precision); TODO-OWNER chips visible in production copy (rail formula, presets); `/new` has no left rail; search shows one section when only one matches; no search box at 390 px; the market page lands on the worst structure in the book by default (`Max payout 0×`); a confirmed position reads "Open · syncing" (lane C #10). The shared Playwright MCP browser is polluted by a stale fake-wallet init script from an earlier tester (port 3197) — use a private headless context for browser work.
- **Lane B SECOND pass (Astra MEDIUM, pin `058e655`, 21:50, `review-confirm/astra-confirm-B.out`): RED — 1 MAJOR regression + 3 MINOR.** MAJOR TRUE at the bytes: fold B r2's `rankedTheses` limit (6) runs BEFORE `following()` and `top()` filter (`lib/social/feeds.ts:24/:30`), so those feeds can be empty while eligible posts exist. → **Fold B r3 DONE and MERGED (`7a41c75` → main `5f278ae`, pushed 21:58):** `rankedTheses` takes `creatorIds`/`statuses` applied in SQL before `ORDER BY`/`LIMIT`; `following()` and `top()` pass them; Claude's mutant (drop the creator predicate) turns the SQL test RED; the writer's live cap test was order-dependent (asserted an exact trending list) — Claude made it membership-based; re-measured: tsc clean, offline 448/0/41, live web 534/7/0 on a fresh 0008 database. MINORs = the known owner items (B-m3 wallet switch, OG emoji via jsDelivr, `listActivity` unbounded). Identity, avatars, fences, search, origin lenses PASSED: generated identity retry/fallback proven (mutant RED), 1,000 avatars in 62.49 ms, client bundle for `avatar.ts` 25,255 B min / 6,799 B gzip, data URIs percent-encoded, seed precedence wallet → id → handle, fences refuse named overrides.
- **2026-09-06 01:18: network outages killed the Opus agents twice (22:57 and ~23:36 on 09-05: API ENOTFOUND). On disk: `fold-C-r3` clean at `14a05ee` — C#1–C#9, D5 and D-C2 ALL COMMITTED (pushed); only the verification + browser pass + report remain → writer RESUMED 01:18. `fold-A-r2` worktree at `dccad66`, nothing done → writer RESUMED 01:18 (sync-script refusals, scripts typecheck). Both branches auto-push on each commit.**
- **Next, in order:** fold C r3 + fold A r2 report → Claude re-measures → merge → push → confirming pass on the folds (lane C is the gate; codex was at capacity 22:46–22:55, retry or Opus leg) → GREEN → production migration 0001–0008 (makes the live site work) → team redeploys. (Was: fold C r2 (Opus) lands → Claude re-measures → merge → push → lane C second pass at the new pin + lane B r2 verdict → GREEN → production migration 0001–0008 → team Vercel. Devfolio: owner's "submit" with the committed screenshots (was: fold C if RED → merge fold B r2 → re-measure → push → production migration 0001–0008 (owner standing rule) → screenshots → owner's "submit". (Was: lane C/B verdicts (running since 21:00 on `e35eb43`; the E merge and requote fix are NOT in that pin — they get Claude spot checks + the merge worker's walk) → fold if RED → screenshots on the merged tree → owner's "submit" → production migration → team Vercel. (Was: merge worker (E) reports → Claude re-measures the merged tree (typecheck, suites on a fresh 0007 throwaway, db build, browser pass) → merge both onto main (Writer B's tree committed by Claude) → confirming Astra passes on all four lanes + a tester re-walk → GREEN → push → production migration → Vercel → the owner's tiny real fill. (Was: ONE fold (Opus HIGH + Astra LOW writers in own worktrees, briefs from the adjudication files + walkthrough + AI hookup + agent-exec merge) → Claude verification → confirming Astra passes B/C/D/A + Opus tester re-walk → GREEN → push main (owner 18:0x: "keep pushing whatever is done … once it's done and green") → production migration → Vercel → the owner's tiny real fill.
- **Production database (measured 18:0x via the session pooler, read-only):** holds exactly migration `0000_agent_tables` (hash `e74f6928c0d5` = ours) and the six `agent_*` tables; `0001`–`0007` will apply cleanly. The session-pooler URL (owner 18:0x: pooler host, port 5432 — the only IPv4 route) is stored as `PROD_DIRECT_DATABASE_URL` in gitignored `apps/web/.env`, password URL-encoded; it is used ONLY for the manual migration after GREEN.
- **Vercel is the TEAM's (owner 19:5x: "vercel side my team will handle no need worry this for now").** Claude does not touch Vercel. Hand-off for them: `docs/TEAM-HOSTING.md` (from `vercel.json`, the env contract and today's measurements: one project at the repo root, `DATA_SOURCE=db` at BUILD time, the 6543 pooler as `DATABASE_URL`, `SESSION_SECRET`, `OPENROUTER_API_KEY`; migrate from a laptop via the session pooler BEFORE the first deploy; the custom domain sets the share-link origin). The review-GREEN gate before any deploy is unchanged.
- **Devfolio (owner 19:5x: "lmk what is needed to submit … Dun submit first tho"):** NOT submitted. A draft project "Thesis.fun" (`thesisfun-68be`, team 爱nuts, 2 members) already exists on `muba-hackathon` with every field empty. Requirements read live from Devfolio's guide and the checklist + draft copy are in `docs/DEVFOLIO-SUBMISSION.md` (name/tagline ≤50 chars, 1–10 hashtags, 1–6 REAL screenshots, two required organizer fields "The problem it solves" and "Challenges we ran into", tracks: Thetanuts SDK track + AI × OPTIONS only if the agent execution is merged and demonstrated, repo link, optional video/cover). Screenshots for it: fresh set from main's db-mode build in `.research/thetanuts/ui-fold-shots/ship-*.png` (copy into `docs/screenshots/` when the owner picks).
- **Owner items open:** OpenRouter credits for the agent key; Devfolio tagline/copy and the demo video (tracks decided: both Thetanuts tracks); avatar style (Claude picked DiceBear `thumbs`; `identicon`/`shapes` are the alternatives) and the generated-name shape (`thesis-NNNN`, four digits, five retries); Vercel project link + env values (`SESSION_SECRET`, `DATA_SOURCE=db`, the 6543 pooler `DATABASE_URL`, `OPENROUTER_API_KEY`); referrer wallet whitelist by Thetanuts + gas on `0xd5E6…47dd`; the tiny real fill (also proves non-USDC contract units and unlocks the gated buy orders); product calls — verified badge for a post linking a standalone trade (schema cannot link it), Bull/Bear naming on a post-less position, `tiny-fill.ts` fee check strict vs upper bound, whether the teammate's `origin/agent-exec-r1` joins the review, mobile ticket hidden ≤1180px by the mockup, accented-letter slugs; every `TODO-OWNER` (presets `$50/$100/$500/$1,000` in code vs `$10/…` in the mockup, rankings, limits, copy, card width, percent basis).
- **Verified facts that gate the code** (all from chain bytes or the SDK bytes, this session): raw `isLong` is the maker's long flag (`true` → taker SELLS); taker-buy pays `contracts × price / 1e8`; taker-sell PHYSICAL_PUT + aBasUSDC posts `strike × contracts / 1e8`; contract units 1e6 for 6-decimal collateral; both fee branches fire; live book after the side fix: buy USDC 170 + aBasUSDC 30 executable, sell aBasUSDC 63 executable, sell aBasWETH/cbBTC gated; OptionBook `0x1bDff855d6811728acaDC00989e79143a2bdfDed`; makers re-sign about every 60 s (teammate-measured, not re-verified).
- **Process facts that bit today (rules now in memory + above):** writers and reviewers each get their own worktree (a mid-review commit to the main checkout aborted a review; `git checkout <file>` in a writer's tree destroyed unstaged edits — recovered from the transcript); the local `postgres` database is a shared throwaway that lags behind migrations — every live run uses its own migrated database; `next build`/`next start` are production and refuse mock fixtures (build with `DATA_SOURCE=db`); `@nuts/env/load` fills probe processes from `.env`; wait loops poll the wrapper path + the `tokens used` footer; a `tokens used` footer does not prove a codex run finished its work; both accounts hit session limits today (codex 12:12–13:15; Claude ~13:1x and ~15:0x) — killed subagents are resumed with "continue, don't redo", their on-disk work survives; Claude committed one red merge before its checks finished (repaired next commit) and once listed a known data gap (no price history) as an afterthought instead of before building — never again.
- **History** (rounds, verdicts, hashes) lives in git log, `.research/thetanuts/review-ledger.md`, and the memory index; this section stays a snapshot.
