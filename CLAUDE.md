# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

`AGENTS.md` at the repo root is a symlink to this file so Codex and other agents read the same guidance. Edit this file only. Keep the **Current state** section at the bottom true; update it whenever something lands or changes, with the date.

## Product source of truth

`docs/PRD.md` is the source of truth for product scope, user journeys, team ownership, shared interfaces, safety rules, and acceptance criteria. This file remains the source of truth for repository conventions and implementation guidance. If the two conflict on product behavior, follow the PRD.

## What this repo is

**Thesis.fun** for the Thetanuts hackathon: a social trading feed in the shape of pump.fun and fomo, where each post ("callout") is a thesis backed by a real options position on Thetanuts, filled from the creator's own wallet on **Base mainnet**. Others take the Bull side (same structure) or the Bear side (opposite) with their own budget. Every position and result is verifiable onchain; losing theses cannot be deleted.

Owner decisions (all 2026-09-05):
- **Every market Thetanuts has liquidity for.** Derive assets, strikes, expiries live from OptionBook orders. Never hardcode an asset list.
- Base mainnet only (chainId 8453).
- **Social trading app like fomo.** Icon rail, creator ticker, callout feed with the position card nested inside each post, trending rail, trader popups, follow and share.
- **A thesis is a post, like X (owner 2026-09-05: "thesis doesn't really need user to actually put a trade first … a pure text opinion is fine also", "allowing users to like that particular thesis also").** Text is required; a tradable structure (market, strikes, expiry, direction) is optional; a backing position from the creator's wallet is optional. Bull/Bear show only when a structure is named. A backed thesis gets the verified badge and the live position card. Likes, comments, follows on every thesis. Consequences, in order: DB round 7 (structure columns nullable, creator position optional, `likes` table, the public-thesis position CHECK relaxed to "if linked, then fenced"), PRD §3/§8/§12 sentence "backed by a real position" becomes "optionally backed" (tell the teammate; PRD §15), mockup (unbacked post state), UI. The AI context for a thesis without a position stays `no_creator_position`.
- **Creator payouts are FUTURE ROADMAP** (owner 2026-09-05: "we'll put creator fees as future roadmap, but we'll do callouts anyways for now. so we just do our own earning first"). Callouts ship; the platform's own referrer share is the only revenue for now. Do not build a creator ledger or payout UI yet; keep the attribution data (fill event, referrer, thesis link) so it can be added later. The rate remains `TODO-OWNER` if and when it comes back.
- **AI track (owner ruling 2026-09-05, superseding the companion-only decision):** the teammate's approval-gated trading agent stays as built ("keep how they made their ai stuff also. is fine"); `docs/PRD.md` v2.0 stands for the AI track, including its §10.2 safety limits. **Rewire their code onto ours wherever possible** (`packages/thetanuts` for orders, quotes and fill calldata; `packages/db` for thesis context). AI never signs; the wallet approves every transaction.
- **No Privy.** Wallet address is the identity (wagmi + viem, include Coinbase Smart Wallet). Add email/social login only if the owner asks. **Connecting a wallet creates the user profile** (owner 2026-09-05): create-or-fetch the `users` row keyed by the lowercase address, idempotent; build step 4.
- **Both sides (owner ruling 2026-09-05, "do both, go")**: the product offers the taker-BUY side (user pays premium in the order's collateral token; bounded loss) AND the taker-SELL side (user receives premium and locks collateral; loss up to the collateral), each clearly labelled with its own sizing and copy. The sell side stays gated in code until a tiny real fill measures the seller's actual debit (research open question 2); the OptionBook contract source is read first to derive the expected debit.
- **Revenue = Thetanuts referrer fees** (docs "Referrer Fees"): every fill carries a `referrer` address (the builder key); protocol fee `min(0.06% notional, 12.5% premium)`; our share `fee × splitBps / 10000` accrues per collateral token in `fees[token][referrer]` and is self-claimed (`claimFees`). **The OptionBook owner must whitelist our address and set the split** (`setReferrerFeeSplit` is owner-only; unwhitelisted referrers earn 0, seen live in fill `0xa2edb8…`, `referralFeePaid: 0`). RFQ referral fees are owner-withdrawn only. Owner action: get one platform address whitelisted. Creator payouts: roadmap only (decision above). **Roadmap line (owner 2026-09-05): "our platform earns through trading fees" = the referrer share of Thetanuts OptionBook fees; requires the whitelist above.** Memo done (Astra medium, citations to docs/SDK lines and tx hashes): `.research/thetanuts/monetisation-memo.md`. Its recommendation: one platform-controlled referrer address, self-claim per token, an auditable creator ledger credited from each fill's actual `referralFeePaid`; no extra platform charge. Owner decisions pending: the referrer wallet (`THESIS_REFERRER`, custody), the split Thetanuts grants, the creator rate.
- Product numbers are the owner's: budget presets, trending and ending-soon rules, leaderboard formula, creator payout rate, fees, slippage, gas headroom. Tag them `TODO-OWNER` in code and UI. Never invent a value.

Team: owner + Claude on UI and Thetanuts logic. Teammate on the AI track.

Scope conflict of 2026-09-05 (teammate's PRD v2.0 vs companion-only) resolved by the owner the same day: the AI track as built stands (bullet above). The shared `ThesisAiContext` contract (PRD v2.0 §10.3) is field-identical to v1.0 §10.2; `packages/db` implements it exactly.

## Design direction (REDONE 2026-09-05 14:3x: fomo's feel)

- **Owner 14:3x (verbatim): "is it me or the ui/ux kinda suck ass again? idk. i still like fomo's feel. it's more classy" → "go with fomo's feel, redo the mockup now in parallel".** The stadium/gold rules below are SUPERSEDED once the new mockup lands (design round 1, Opus, `brief-design-r1.md`, worktree `design-r1`). New direction: calm dark ground `#0b0b10`, surfaces `#14141b`/`#1b1b24`, hairline `#25252f`, ONE accent (fomo violet `#6f5cff`) for primary buttons/active tab/share-card frame, gain `#22c55e`, loss `#f4634f` (colour only on money), `Manrope` everywhere with `tabular-nums` (no mono, no Bricolage), base 14px / post body 16px / hero P&L 44–56px, cards radius 18 / rows 12 / chips 999, padding 20px, hairlines not shadows; fomo's chrome (slim top bar, horizontal nav Feed · Markets · Leaderboard · Portfolio, persistent left feed rail, right panel for ticket/positions/top traders, avatars everywhere); posts look like a person said something; the share card (owner + status chip + date, instrument, huge signed P&L + percent, three stat tiles) is the position page hero, the post-fill dialog and the trade card in posts; no charts, no ticker tape, no icon rail, no gold. UI rebuild follows the mockup.

## Design direction v1 (decided 2026-09-05 05:xx — SUPERSEDED, kept for history)

- **Spec is the mockup**: `docs/mockups/thesis-fun-mockup.html`. Its `:root` tokens, fonts, layout and charts are the source of truth. Reference material (fomo demo video, pump.fun screenshots) lives in gitignored `.demo/`.
- **Stadium, dark, gold.** Near-black ground `#0e0e11`, surfaces `#15151a` / `#1c1c22`, hairlines `#26262e`. Accent gold `#F5C542` with dark text on filled elements. Gain text `#5ee39a`, loss text `#ff7a8a`; saturated fills `#22c55e` / `#f43f5e` only for chips, chart pins and selected side. Rejected: fomo purple, pump.fun mint `#44F4AB`, orange-and-serif, light "tote board".
- **Fonts**: Bricolage Grotesque (display 800), Archivo (UI), JetBrains Mono (all numbers, `tabular-nums`).
- **Color rules** (from pump.fun, fomo, Stocktwits, Mercury, Kraken, Robinhood): color is for numbers only; labels, names and bars stay neutral. One thin single-color Bull split bar with a neutral remainder, never green-versus-red bars. Bull and Bear buttons are neutral at rest and colored on hover or selection. Gold only where the mockup puts it: the logo, Create and primary/sign buttons, live chips, the active tab, the CREATOR badge, the spot-price guide line and the spot chart's gradient, plus focus outlines. (Corrected 2026-09-05 after review; the creator-earnings dot is gone with the placeholders.) Small mono meta text at 12px, not 11px.
- Design changes go through a mockup first (pixels before prose), then code.
- **Market page + posts (owner 2026-09-05, from the fomo demo):** trading lives on a per-asset market page (price chart, live option structures from the book, Bull/Bear trade panel); posts are social (like, comment, follow, share) and may tag a market or structure; no trade buttons inside posts. Mockup of the market page and the unbacked-post state comes before code.
- **NO PRICE CHART on the market page (owner 2026-09-05 14:1x, superseding the chart order below).** Thetanuts exposes only the live spot price (SDK `getMarketData`), no history; Claude built the market chart on a generated series labelled "example" and surfaced the gap only when asked "what's left" — owner: "the fuck thetanuts got no chart history? then why u didn't tell just now. remove the chart then if this the case … think for the users man". The chart, its window buttons and `lightweight-charts` are removed; spot + 24h change stay as numbers. **Lesson (standing rule): a brief that rests on data that does not exist or is unverified says so to the owner in the same message that proposes the build, with the user-facing consequence — never as an afterthought.**
- **A trade is just a trade; a post is just a post (owner 2026-09-05 14:2x, verbatim: "trade is just trade. post(thesis) is it's own thing. doesn't have to be tied. but when a user post it's trade (could be a link to his own live pnl card) in the trade then we render a nice card. u know how like if i share my own posts' link in x, x will render the posts' as a card? and not just the link? and it's clickable cuz issa link, then bring me to that card kinda thing?").** Positions are independent rows (migration `0007_standalone_positions`: `positions.thesis_id` nullable, `role = 'standalone'`, CHECK tying the two; the frozen creator-invariant triggers still reject a standalone row as a creator position) with their own page `/p/<id>` showing the live P&L card; the ticket never creates a post. A post is text; a same-origin `/p/<uuid>` link in the text renders a clickable trade card under the text (X-style unfurl). `theses.creator_position_id` stays for the verified badge when a post links the author's own confirmed position. Owner: "pleas go code it all out first then we only do reviews."
- (Superseded by the two bullets above) **Charts: TradingView `lightweight-charts` (owner order 2026-09-05).** Verified identity before install: npm `lightweight-charts@5.2.1`, Apache-2.0, repository `github.com/tradingview/lightweight-charts`, maintainers TradingView staff (`tradingview-admin`, `rfrancois`, `edewhursttv`, `slicedsilver-tv`, `subzey`), no install scripts, sole dependency `fancy-canvas@2.1.0` (audited the same way), tarball SHA-256 `64310292298df4dd527494865e5d91d447f8bb397b68de3912c2a6465025ee5e`, shipped code has no network calls, `eval`, or injection text. Install only that name at that version; never a look-alike.

## Thetanuts integration

- SDK: `@thetanuts-finance/thetanuts-client@0.3.0` (ethers v6 + viem), installed in `packages/thetanuts`. Docs: https://docs.thetanuts.finance/sdk (append `.md` to a page URL; full export at `/llms-full.txt`).
- **Read `docs/thetanuts-sdk-research.md` before touching the SDK.** It is a 999-line research report written by a codex sol and verified by Claude against the SDK bytes (14 load-bearing citations re-read). Its prompt is beside it.
- Verified facts that shape the code:
  - `encodeFillOrder` and `encodeApprove` return `{to, data}` calldata; wagmi/viem send them. No ethers signer needed for the app.
  - `client.api.fetchOrders()` returns every live maker order; assets come from `buildPriceFeedSymbolMap(8453)` (eight feeds configured: ETH, BTC, SOL, DOGE, XRP, BNB, PAXG, AVAX).
  - In a browser the client constructor throws unless `keyStorageProvider` is passed; use `MemoryStorageProvider`. RFQ is never used.
  - **Raw `isLong` is the MAKER's LONG flag — CORRECTED 2026-09-05 14:4x from chain bytes (`.research/thetanuts/finding-taker-side-inverted.md`): `isLong: true` → maker is the buyer → the TAKER SELLS and posts collateral; `isLong: false` → maker sells → the TAKER BUYS and pays premium.** The SDK comment at `normalizeOdetteOrder` ("isLong=true means maker sells, so taker buys") and its `isBuyer = !isLong` are WRONG; `packages/thetanuts/src/side.ts` was inverted until core round 9. Proof: tx `0x9c4bb1…` (`isLong=false`, taker = `OrderFilled.buyer`, paid 999998 USDC premium) and `0xdf3323…` (`isLong=true`, taker = `seller`, posted 22,000,000 aBasUSDC). Live book 2026-09-05 01:47 UTC (337 orders), corrected: isLong=true 125 = taker-SELL (aBasUSDC/aBasWETH/cbBTC collateral), isLong=false 212 = taker-BUY (182 in USDC, 30 in aBasUSDC). The teammate's agent "tradeable set" (`isLong === false` + USDC, "long option … most that can be lost is the premium") was RIGHT; Claude's `finding-isLong-side.md` is retracted. Every earlier statement of the opposite in this file is superseded by this bullet.
  - The budget passed to preview/fill is the taker's **premium spend** in collateral base units, not contracts. Contracts are capped at the maker's remaining size. Recompute premium as `numContracts * pricePerContract / 1e8`; never trust `preview.totalCollateral`.
  - Preview returns no max loss or max payout. Settlement is automatic on r12; `client.option.payout()` throws.
  - Do not use: `filterOrders` (drops `rawApiData`), `client.events.getOrderFillEvents` (stale event layout), `mmPricing` beyond ETH/BTC, `swapAndFillOrder`, RFQ, MCP.
- **Fill debits VERIFIED from decoded production fills 2026-09-05** (`.research/thetanuts/finding-fill-debits.md`, tx hashes inside): taker-BUY pays premium = numContracts × price / 1e8 in the collateral token (fee 12.5% of premium carved out of what the maker receives; maker posts strike × contracts or spread width × contracts); taker-SELL (put) posts collateral = strike × numContracts / 1e8 in the collateral token and receives premium − fee. `numContracts` is in 1e6 units for USDC/aBasUSDC orders (10000 = 0.01). Sell-side CALL collateral (SDK doc: 1:1 underlying) still needs a decoded example.
- **UNVERIFIED until tested with small real money** (see the report's open questions), after today's decoded fills: contract-size units for non-USDC-family collateral (cbBTC, aBasWETH); the taker-sell debit for anything other than PHYSICAL_PUT + aBasUSDC (calls, spreads, RANGER as seller); capped-budget rounding on chain; the fee's notional branch; settlement timing and indexer P&L accounting. Code that touches these says so in its doc comment and stays gated.
- `@thetanuts-finance/mcp` was audited clean but is for AI chat clients; the owner ruled it out for the app.
- **Teammate-measured on Base mainnet 2026-09-05 (`docs/HANDOVER.md` §5; NOT re-verified by Claude):** maker signatures expire in 59–113 s and are re-signed about every 60 s, so approve collateral first, then re-fetch and build calldata within ~30 s; the SDK WebSocket host does not resolve (poll 20–30 s); `availableAmount` is the maker's collateral budget, not contracts; the book carries binaries (e.g. "ETH 2460 Up 1D"); tradeable set is USDC-collateral orders with `isLong: false` (~200 of 367); position read-back after a fill needs `api.triggerIndexerUpdate()` and polling; the seven `PHYSICAL_*` multi-leg implementations are not deployed on Base; RFQ has had no offers since 2026-08-20. Treat each as a hypothesis until our own tiny fills confirm it.

## Build order (owner-approved 2026-09-05, revised the same day)

Owner's order: **UI first, then Thetanuts core logic, then DB and socials.** Concretely:
1. **Core trade logic** in `packages/thetanuts` (framework-agnostic) — buy side DONE (GREEN); sell side in review: read client, market universe from live orders, quote with recomputed premium, approve + fill calldata for viem, receipt parsing, position readers, payoff math. Offline tests.
2. **UI on typed mock data** in `apps/web` from the mockup: `/`, `/t/[slug]`, `/u/[handle]`, `/portfolio`, `/new` — DONE (rounds 1–5 GREEN, merged).
3. **Wire UI to logic + wallet** (wagmi + viem, Coinbase Smart Wallet; providers already in place from the teammate) — NEXT, after the rewire. Real fills with tiny size on mainnet to settle the UNVERIFIED list.
4. **Foundation + socials**: Drizzle schema (users, theses, positions, follows, comments, activity) — DONE (GREEN, merged); still to do: sign-in with wallet (connect creates the profile), follow, comment, activity, leaderboard, trending. Creator payouts: roadmap.
5. **Polish and ship**: Open Graph share cards, verified badges, Vercel.

Parallel track (teammate): AI track per `docs/PRD.md` v2.0 (`/agent` workspace, `apps/web/src/lib/thetanuts/*`, `packages/db/src/schema/agent.ts`, migration `0000_agent_tables`). Scope ruling above.

Shared contract with the teammate: `docs/PRD.md` v2.0 §10.3 (v1.0 §10.2) defines `ThesisAiContext`; the core side (us) builds and validates it and provides a fixture plus server function. Neither side changes it without telling the other and updating the PRD (PRD §15).

## How work is done here (owner rules, verbatim where it matters)

- **"NEVER TRUST YOURSELF, DON'T FUCKED UP."** Every claim, number, name and file path is verified at the source before it is used or said. A sol's output and Claude's own output are invalid until re-measured. Load the `verify-first` skill before claims and the `fable-method` skill before hard tasks.
- **Zero confidence in everyone, including yourself** (owner, verbatim: "none of yall should trust yourselves or the other. zero confidence in everyone's responses and findings"). Every brief carries this clause; every claim in a report is pasted measurement or it is not a claim; the orchestrator re-measures every headline number and every finding before folding or relaying it.
- **CODE FIRST, ONE-SHOT REVIEW (owner 2026-09-05 13:2x, verbatim: "let's skip all reviews. code ito ut first. then on shot review. keep in track what to review can already").** Refined by the owner minutes later (verbatim): "one opus high reasoning one astra low reasoning to code everything out first. then due to not trusting them, we review and verify. but don't trust yourself too later btw / also the reviews should do all under one astra medium and yourself, try to one shot the review also so it takes lesser rounds to reach where we want. i'm not asking we should lower our work quality btw". So: writers = ONE Opus agent at HIGH reasoning + ONE codex `gpt-6-astra` at LOW, in parallel, own worktrees; the review = the USUAL two-leg review (Astra MEDIUM + Claude hands-on, adversarial, digest-pinned, fold until GREEN), run ONCE after all the code is finished (owner 13:4x, verbatim: "the one shot review is meant for you to complete all the code then do the usual 2 leg review is what i meant. except we finish the work first then only review instead of plan -> code -> review -> plan fix -> code -> repeat until green which takes a lot of time"); Claude's own verification is re-measured in that pass too. No per-round adversarial reviews until the owner calls the pass. Claude still verifies every writer's claims (typecheck, tests, build, spot-checks at the bytes) before committing and merging — that is verification, not review. Everything merged unreviewed, and every carried finding, is logged in `.research/thetanuts/review-ledger.md` with what the one-shot review must attack. Nothing reaches production before that review is GREEN — owner 13:4x, verbatim: "the one shot review goes before we push for actual db migration on prod's db." Order: code → Claude verification → one-shot review GREEN → production `drizzle-kit migrate` (never `push`) → Vercel → real-money fills. The paragraph below describes the review shape when it runs.
- **Writer and reviewers are different agents.** Owner rule 2026-09-05 ("all towards gpt astra low effort and we review that"; earlier "don't use opus to work things not related to ui"): code is written by Opus workers (Agent tool, own worktree, fable-method first) AND codex `gpt-6-astra` at low reasoning (`codex exec -s workspace-write -m gpt-6-astra -c model_reasoning_effort="low"`; Astra needs codex CLI ≥ 0.153, a Homebrew cask, `brew upgrade --cask codex`), workload balanced between the two (owner 11:20 +08, superseding the earlier "no Opus for non-UI"), always from a pinned brief carrying the no-product-decisions block. Review is two legs: Claude (hands-on, adversarial, not the author) and codex `gpt-6-astra` at **medium** reasoning — **while codex is out of credits (from 2026-09-05 12:12 +08 until Sep 10 15:01, codex's own message) the second leg is a fresh Opus adversarial agent, decided by Claude and flagged to the owner** — (owner 2026-09-05: "medium for reviews should be enough, low for work") with a digest-pinned diff and a mandatory `Reviewed <path> SHA-256 <digest>` opener. Fold until both are GREEN. The codex sandbox has no network and no database and cannot write a worktree's git lock (it lives under the main repo's `.git`), so writers leave the tree uncommitted; Claude verifies, runs live tests and builds, and commits. Briefs and transcripts live in gitignored `.research/`.
- Parallel writers get separate git worktrees. Never two agents in one mutable tree.
- **Wide fan-out (owner 2026-09-05: "do everything at once so we spend lesser rounds … instead of brick by bricks")**: launch every independent piece in parallel with complete briefs (all known findings folded up front, completeness checklist), review with all lenses in one round, fold until GREEN.
- **Bun only.** Never npm, npx, yarn, pnpm. Registry lookups via `bun pm view`. shadcn via `bunx shadcn@latest add <name> -c packages/ui`.
- Never push without an explicit owner command. Never `--no-verify`. Local commits are fine.
- **Pull from GitHub whenever the team has pushed** (owner rule 2026-09-05): `git fetch origin` at the start of every work block and before every commit or worker launch; merge `origin/main` if it is ahead; re-read changed guidance (`CLAUDE.md`, `docs/PRD.md`) before acting; pin worker base commits to hashes on the merged main.
- The repo will be public: no credential-shaped values in tracked files. Env files: `apps/web/.env.local` (real values, gitignored) overrides `apps/web/.env` (gitignored; Claude's local `DATABASE_URL`, the production Supabase URL and password); `apps/web/.env.example` is the only env file in git and never holds a real value. `packages/env/src/server.ts` now also requires `OPENROUTER_API_KEY` (teammate's agent); local runs need a placeholder value in `.env`.
- Teammate's rule (their CLAUDE.md, 2026-09-05): they push validated checkpoints to feature branches under a standing approval they record from the owner; that approval is not approval to merge to main. Claude's own push rule above is unchanged.

## Commands

Package manager is **bun** (`packageManager` pin in root `package.json`). Turborepo drives tasks; `-F <pkg>` filters.

```bash
bun install                 # workspace install (run from repo root)
bun run dev                 # all apps; web is http://localhost:3001
bun run dev:web             # web only
bun run build               # turbo build
bun run check-types         # tsc --noEmit across packages
cd packages/thetanuts && bun test          # offline unit tests for the trade logic
cd packages/thetanuts && bunx tsc --noEmit

# Database (Drizzle + Postgres, run from root)
bun run db:push             # LOCAL THROWAWAY DATABASES ONLY - see warning below
bun run db:generate         # write SQL migrations to packages/db/src/migrations
bun run db:migrate          # apply migrations
bun run db:studio           # Drizzle Studio

# Local Postgres via Supabase CLI (Docker), config in packages/db/supabase/
# Only the db (and its kong gateway) are enabled; auth, storage, studio, realtime,
# inbucket, edge runtime and analytics are switched off in config.toml on purpose.
cd packages/db && supabase start     # DB on postgresql://postgres:postgres@127.0.0.1:54322/postgres
cd packages/db && supabase status
cd packages/db && supabase stop

# The turbo db:* wrappers are marked interactive and fail without a TTY (agents, CI).
# Run drizzle-kit directly instead. No --env-file needed: see Env loading below.
cd packages/db && bunx drizzle-kit generate --name <change>   # write the migration
cd packages/db && bunx drizzle-kit migrate                    # apply it

# NEVER run `drizzle-kit push` against the shared Supabase project. push reshapes
# the database to match the schema of whoever runs it, so running it from a tree
# that lacks the other developer's tables can DROP those tables. Migrations are
# additive and reviewable; push is for a local throwaway database only.

# Vercel
bun run deploy:setup        # vercel link (once)
bun run env:preview | env:production   # push apps/web/.env to Vercel (scripts/sync-vercel-env.ts reads .env only, NOT .env.local)
bun run deploy | deploy:prod | deploy:check
```

No lint setup yet; `turbo.json` declares a `lint` task nobody implements.

## Architecture

Turborepo monorepo, all TypeScript, ESM. Workspaces: `apps/*` and `packages/*`. Shared dependency versions live in the root `package.json` `catalog` and are referenced as `"catalog:"`.

```
apps/web             Next.js 16 app (App Router, React 19, React Compiler on, typedRoutes on)
packages/thetanuts   @nuts/thetanuts: framework-agnostic trade logic on the Thetanuts SDK (+ bun tests)
packages/db          Drizzle ORM + node-postgres. Schema in src/schema, migrations in src/migrations
packages/env         Validated env via t3-env. `@nuts/env/server` (DATABASE_URL, DIRECT_DATABASE_URL?, NODE_ENV, OPENROUTER_API_KEY, AGENT_MODEL, AGENT_GATE_MODEL, BASE_RPC_URL, THETANUTS_ORDERS_URL), `@nuts/env/web`, `@nuts/env/load` (repo-relative .env.local/.env loader)
packages/ui          Shared shadcn/ui components on @base-ui/react + Tailwind v4. Exports globals.css, components/*, lib/*, hooks/*
packages/config      Shared tsconfig.base.json (strict, noUncheckedIndexedAccess, noUnused*)
docs/                Tracked knowledge: PRD.md (v2.0), HANDOVER.md (teammate), SDK research report, UI mockup
scripts/             sync-vercel-env.ts (used by env:preview / env:production)
.research/, .demo/   Gitignored: Astra/sol briefs, transcripts, review diffs, findings; reference videos and screenshots
```

How the pieces connect:
- **Env files**: `apps/web/.env.local` holds real values and is gitignored; `apps/web/.env.example` is committed and documents every variable. `.env.local` overrides `.env`, matching Next.js precedence, and the real process environment beats both so Vercel and CI are unaffected. Copy to start: `cp apps/web/.env.example apps/web/.env.local`.
- **Env loading**: `@nuts/env/load` resolves the files relative to the repo, not the current working directory, and every entry point imports it. A script run from `packages/db` finds the same values the web app sees, so `bun --env-file=...` is no longer needed. Required today: `DATABASE_URL` and `OPENROUTER_API_KEY`.
- **Two database URLs.** `DATABASE_URL` is Supabase's transaction pooler (port 6543) and is what the app uses: Vercel functions open many short-lived connections and would exhaust a direct connection. `DIRECT_DATABASE_URL` is the direct connection (port 5432), used only by drizzle-kit because schema changes cannot run through the pooler. On a local database one string does both jobs and `DIRECT_DATABASE_URL` stays empty.
- **DB access**: import `db` from `@nuts/db`. It is created eagerly from `env.DATABASE_URL`, so importing `@nuts/db` in a client component will fail. Keep it in server components, route handlers, and server actions.
- **Env validation** runs at import time. `next.config.ts` imports `@nuts/env/web` so bad client env fails the build. `SKIP_ENV_VALIDATION=1` bypasses server validation.
- **Bun installs into an isolated store**: packages resolve through `node_modules/.bun/<name>@<version>/node_modules/...` and per-package `node_modules` symlinks, not a flat root `node_modules/<name>`.
- **UI imports**: `import { Button } from "@nuts/ui/components/button"`. The web app's `index.css` imports `@nuts/ui/globals.css`; design tokens live in `packages/ui/src/styles/globals.css`.
- **Path aliases** in `apps/web`: `@/*` → `apps/web/src/*`, `@nuts/ui/*` → `packages/ui/src/*`.
- **Providers**: `apps/web/src/components/providers.tsx` wraps the app in the teammate's `WagmiProvider` (`apps/web/src/lib/wagmi.ts`: Base only, injected + Coinbase Smart Wallet, cookie storage) and `QueryClientProvider`, then the forced-dark next-themes `ThemeProvider` and the sonner `Toaster`.
- **Vercel**: `vercel.json` deploys `apps/web` as the single `web` service with a root-level `bun install`.

## Next.js version warning

`apps/web/AGENTS.md` is auto-generated by `next dev` and says this Next.js 16 differs from training data. Before writing Next.js code, read the relevant guide under `apps/web/node_modules/next/dist/docs/`. Do not delete that file; `next dev` re-creates it.

## Current state (keep this true; last updated 2026-09-05 16:25 +0800)

- **Codex is back (owner 13:15: "astra is back"; PONG probe OK). The Claude account hit its own session limit at 13:1x (an Opus subagent died with HTTP 429 mid-fold); the owner switched accounts; the subagent was resumed with "continue, don't redo" and its on-disk work survived.** Earlier outage record: **CODEX OUTAGE (12:12 +08)**: every `codex exec` returns "You've hit your usage limit … try again at Sep 10th, 2026 3:01 PM" (probe re-confirmed). Three Astra runs died mid-flight: the server-layer review (no verdict), the DB round-9 writer (its edits landed, its typecheck never ran; Claude verified and committed them), rewire round 4 (never started). A `tokens used` footer does not prove a run finished its work. Interim until the owner buys credits or the limit resets: Opus adversarial agents are the second review leg, Opus workers write. Relaunched on Opus at 12:3x: DB review r9 (`brief-db-review-r9-opus.full.md`, own database `review_db_r9`), UI review r6 (`brief-ui-review-r6-opus.full.md`, port 3111), server review r1 (`brief-server-review-r1-opus.md`, port 3110, shared `postgres` database read-mostly), rewire r4 writer (`brief-rewire-r4-opus.md`, worktree `rewire2`).
- **Rewire rounds 2–5 merged into main at `a11fcf0` and pushed** (Claude verified: check-types 3/3 forced, tsc clean, web 64 / thetanuts 46 tests, `next build` OK). Round 5 (Opus writer): explicit collateral → USD map (`COLLATERAL_USD_SOURCES`: stablecoins at a 1 USD peg `TODO-OWNER`; wrapped majors REFUSED because the SDK has no citable token → underlying relation), `feed_unusable` structured error, `kind` filtered before the 200-row cap (`TODO-OWNER`), sell-unit asymmetry closed, `asOf` on every preview, `quotedOrders`; live: 62 of 325 orders executable (all aBasUSDC taker-BUY puts), was 0. **Unreviewed under the code-first rule — in the ledger.**
- **Server layer rounds 1–2 merged into main (`f7665d5` merge; `1180e38` duplicate-dependency repair; `f2ddeb9` thesis-context reader injection) and pushed.** Claude verified: tsc clean, web suite **174 pass** on an own database `claude_srv2`, both builds (`ƒ /t/[slug]`, `ƒ /u/[handle]` in db mode). Two merge repairs by Claude are in the ledger: the duplicated `@nuts/db` key (and a stale `.next` cache that kept reporting the old JSON error), and `src/lib/thesis-context.ts` now takes an injectable `ThesisContextReader` because the rewire test's `mock.module("@nuts/db")` was global to the bun process and made `db.transaction` undefined for the auth/reads integration tests (6 failures only in the full run). `OPENROUTER_API_KEY` (owner, 13:3x) lives only in the gitignored `apps/web/.env` (+ worktree copies); proven absent from every tracked file.
- **Polish round 1 merged into main (`c03b64e` merge + `230f100` fixture fix) and pushed.** Migration chain is now `0000`–`0005` (`0005_slugs_and_handles`: `theses.slug` NOT NULL unique, backfilled from the headline + id hex; `users.handle` nullable unique; bounds `TODO-OWNER`), `packages/db/src/slug.ts` (`deriveSlug`), Open Graph + Twitter PNG cards on `/t/[slug]`, `SESSION_SECRET` required in production (so `next build` needs one: the gitignored `apps/web/.env` now carries a random local dev secret; Vercel needs its own) and `DATA_SOURCE` in `@nuts/env`. Claude verified live on `claude_polish`: backfill on 4 seeded rows incl. a collision, CHECKs, 339 DB tests, 178 web tests, both builds, PNG served with `og:image`/`twitter:card`. Claude repairs (ledger): the concurrency fixture's `$1::text` uuid deduction, merge conflicts in `/t/[slug]` and `/u/[handle]` (kept `force-dynamic`, dropped `generateStaticParams`, `generateMetadata` and `thesisShareData` now read through the mode-aware `thesisDetailData`/`creatorPageData`; db-mode cards never claim "verified" until the view exposes confirmation), and `slug`/`handle` added to three web test fixtures. (Slug/handle follow-up: done in identity round 1.) A build is committed only after typecheck, live tests and both builds pass — Claude broke that order once at 13:4x (committed the polish merge before the checks finished) and repaired it in the next commit.
- **Socials round 1 merged into main at `88f8c92` and pushed** (Astra low writer; handoff in `apps/web/src/lib/social/REPORT.md`): like/follow/comment server actions with session + uuid + public-thesis fences and atomic `activity` rows; `listActivity`; leaderboard (1W confirmed-window P&L), trending (likes + comments + filled participants), ending, settled — every formula `TODO-OWNER`; optimistic UI with rollback. Claude verified on main: check-types 3/3, web **198 tests** live on `claude_polish`, both builds, DB 339, thetanuts 46. Claude repair: `slug` added to its integration fixture. Writer gaps (ledger item 7): follow activity needs a schema change (`activity` CHECK requires thesis_id or position_id); Ending/Settled tabs static; expired theses have no display state; no reusable sign-in trigger; comment cap and pagination `TODO-OWNER`.
- **Identity round 1 merged into main at `9ed06b7` and pushed** (Astra low; handoff `apps/web/src/lib/data/REPORT.md`): server data layer routes on `theses.slug` / `users.handle` (uuid and address still accepted, slug pattern validated before any query), `identity.ts` claims corrected, migration `0006_follow_activity` (`activity.target_user_id` + FK + index, CHECK re-created to allow target-only rows), follows record one activity event. Migration chain now `0000`–`0006`. Claude verified on main: check-types 3/3, drizzle check, DB **341** live, web **202** live, both builds, thetanuts 46. The slug follow-up from polish is closed.
- **In flight under the code-first rule (13:4x)**: Opus HIGH **trade wiring round 1** in `.claude/worktrees/trade-r1` (branch `trade-r1` from `f2ddeb9`; `brief-trade-r1.md`): live markets from `@nuts/thetanuts`, ticket → quote → approve + fill via wagmi → `recordTrade` (receipt via `expectOrderFilled`, `positions` row, backing links + publishes the post in one transaction), composer publishes text/tagged posts, own database `trade_r1`, port 3115; product reading in the brief: every position belongs to a post (schema), Bull/Bear on someone's post via `?thesis=&side=`, backing your own post via the "Post about <asset>" panel, standalone trades `TODO-OWNER`. (socials round 1: landed, see above). **Earlier in flight (13:2x–13:3x)**: Opus **server round 2** resumed in `server-r1` (fold of the RED r1 review + merge of main; its on-disk state before resume: 68 files, tsc clean, 138 mock-mode tests); Astra low **polish round 1** in `.claude/worktrees/polish-r1` (branch `polish-r1` from `a11fcf0`; `brief-polish-r1.md`): migration `0005_slugs_and_handles` (`theses.slug` unique + backfill from the headline + 4 id hex like the mockup's `btc-nfp-4a2c`; `users.handle` nullable unique, no writer yet), Open Graph cards on `/t/[slug]` via `next/og`, `SESSION_SECRET` + `DATA_SOURCE` in `packages/env`, Vercel readiness report. Next launches once server r2 lands and merges: Opus **trade wiring** (market page ticket → `@nuts/thetanuts` quote → approve + fill via wagmi → receipt → `positions` row; composer `/new` writes theses; profile on connect) and Astra low **socials** (like/follow/comment/activity writers, leaderboard + trending queries with `TODO-OWNER` formulas).
- **`main` = teammate's AI track (origin/main `c30a4e2`) + UI rounds 1–4 (`b88b580`) + DB rounds 1–6 (`0fbd4d6`).** Pushes: `c30a4e2..cda192d` (11:01, DB GREEN), `cda192d..ba0e3d0` (11:4x, UI r5 GREEN + docs), `ba0e3d0..d63985c` (11:47, core sell side GREEN + tiny-fill script) after full verification and secret scans. **Standing rule (owner 11:40): push main whenever work is GREEN from both legs and proven working** (fetch, check-types, all suites live, `next build`, secret scan first); never RED or unreviewed work. Fetch origin at the start of every work block.
- **Process** (owner 11:20 +08): writers are Opus workers AND codex `gpt-6-astra` low, workload balanced between them; reviews are Claude hands-on + Astra medium; every result re-measured by Claude; bun only. Codex sandboxes have no network, no DB, and cannot write a worktree's git metadata at all (no commit, no merge, not even ORIG_HEAD): briefs must never ask a codex writer to run a writing git command; Claude merges/commits, writers leave trees uncommitted. The local Postgres is ONE shared throwaway: Claude's live DB runs reset it, which interrupted a worker testing against it (server-r1 at 11:54); never run two live-DB actors at once, or give a worker its own database. Queue launches with the wrapper counter `pgrep -f '^/bin/bash /Users/aarontan/Developer/-nuts/.research/thetanuts/run-astra-'` (full path: a bare `run-astra-` also matched another project's wrapper). Background WAIT loops must poll `[ -f <out> ] && grep -c '^tokens used' <out>` plus the anchored wrapper path — a wait that polls a bare queue-script name matches its own shell and never exits (seven such waits were stuck on 2026-09-05 12:1x). Briefs pass values via environment variables to Python, never through unquoted heredocs (a backtick once spliced a `git diff` into a brief). A worktree that merged main after UI round 6 needs `bun install` (new `lightweight-charts` dependency) and `bunx next typegen` (typed routes) before `tsc` is meaningful; those 7 errors are environment, not code.
- **Core trade logic** `packages/thetanuts`: rounds 1–4 GREEN at `e8b0b06`; sell side rounds 5–8 GREEN and merged (`d4a4597`). History: round 5 `6cb674e` (`takerSide`, `quoteSellFill`, `buildSellFillTransactions`; review RED: capacity formula used as seller collateral), round 6 `7d536cc` (implementation-specific collateral via `getOptionImplementationInfo`: PUT = strike, RANGER = inner width, PUT_FLY via the SDK helper; `STRUCTURE_COLLATERAL_UNVERIFIED` gate otherwise; 34 tests; Claude: tsc OK, 34 pass). Astra medium review r6 **RED** (`astra-core-review-r6.out`): the RANGER inner-width formula matched the decoded fill by coincidence (inner gap 1000 = 2 × wing 500) and diverges from the SDK collateral helpers elsewhere; the verified-PUT path ignores collateral token/decimals; the decoded sell fill's implementation `0x6aD5…` is PHYSICAL_PUT (so PHYSICAL_* IS deployed on Base, contrary to the teammate's note). Round 7 `c61b5ff`: collateral via the SDK bigint helper, name-based PHYSICAL_PUT exemption; Astra medium review r7 **RED**: the exemption matched the implementation NAME (five addresses share it; only `0x6aD5…` has evidence) and the two decimal arguments cancel (`10^(sizeDecimals − collateralDecimals)`), so non-USDC tokens size like USDC. Round 8 (Opus worker) `d198bb0`: Astra medium review **GREEN** (`astra-core-review-r8.out`; 2 README-wording MINORs carried; nine mutations RED; collateral equal to the SDK helper on 8 fixtures and to the decoded transfers); Claude: 46 pass, two mutations RED. **Core sell side GREEN from both legs; merged into main at `d4a4597`** with the tiny-fill script (`packages/thetanuts/scripts/tiny-fill.ts`, `bun run tiny-fill`; dry-run by default; `--send` needs `TINY_FILL_PRIVATE_KEY` + owner-set `TINY_FILL_MAX_BUDGET`). The worker refuted Claude's brief on decimals with SDK measurements (contract-size unit = 10^collateralDecimals, not fixed 6) and caught a wrong HEAD pin in the brief. Original plan: exemption pinned to (implementation ADDRESS `0x6aD5…02DcA`, collateral ADDRESS aBasUSDC `0x4e65…c0AB`); SDK helper arithmetic verified by Claude at `dist/index.js` ~11017–11060: `collateral = maxPayout × numContracts / 10^priceDecimals / 10^(sizeDecimals − collateralDecimals)` with defaults `DECIMALS2.SIZE`/`DECIMALS2.USDC`, so `sizeDecimals` must be the contract-size unit (6 per the fills) and `collateralDecimals` the token's decimals, passed separately and proven on 6/8/18-decimal fixtures with a RED mutation. Not merged.
- **DB** `packages/db`: **GREEN from both legs at round 9 (`2dfa968` on `db-r7`, merged into main at `239c328`; rounds 1–6 earlier at `0fbd4d6`)**. Chain now `0000`–`0004` (`0003_thesis_is_a_post`, `0004_unbacked_wallet_fence`); 14 tables incl. `likes`. Chain: `0000_agent_tables` (teammate's, applied to production), `0001_core_schema`, `0002_core_triggers` (deferred creator-invariant triggers with `FOR SHARE` reads in users→positions→theses order, conflict touch-writes for repeatable-read writers, current-row re-reads, snapshot immutability, users-wallet fence). Migrate fence in `drizzle.config.ts`: prints the effective target, rejects `?host=`/`?port=`/`dbname` overrides, requires `DRIZZLE_ALLOW_REMOTE=1` for non-local hosts. Live on the local throwaway: 45 offline, 123 integration, 16 concurrency (RC/RR/SERIALIZABLE). Remaining MINOR from r6 review: concurrency assertions do not record which statement failed (fold with the next DB round). Production migration still pending (re-probed 12:50 +08, read-only: `db.<ref>.supabase.co` has only an AAAA record, this machine has no IPv6 default route, `getaddrinfo ENOTFOUND`; `apps/web/.env` holds only the direct `PROD_DATABASE_URL`, no pooler URL). Owner item: paste the Supabase **session pooler** URL (IPv4, port 5432, user `postgres.<ref>`) from Dashboard → Connect into `apps/web/.env` as `DIRECT_DATABASE_URL`, and the transaction pooler (6543) as the app `DATABASE_URL` for Vercel; then Claude runs `DRIZZLE_ALLOW_REMOTE=1 bunx drizzle-kit migrate` after the fence prints that target; never `push`. Production currently has only `0000_agent_tables` (teammate); `0001`–`0004` are pending there.
- **UI round 6 landed `c8b0408` on `ui-r6`** (worker done 12:2x; parent `6eee798`; 23 files, +1791 −527; mockup SHA `34bed961…566f0`): market page `/m/[asset]` (BTC/ETH/SOL) with `lightweight-charts@5.2.1` (identity re-measured by Claude from `bun.lock`: only `lightweight-charts@5.2.1` + `fancy-canvas@2.1.0` added), three post states + working Like, thread page, `/new` with optional market tag, `fills` chain deleted. Claude leg 12:3x: `bun run check-types` 3/3, `tsc` clean, 28 tests pass; browser check done 12:36 (market page: chart centre, ticket right, structures below, gold guide line; home: three post states, Like, no trade buttons; two console 404s = the missing-fixture trending links). Opus adversarial review **GREEN** (12:3x; zero MAJOR; 6 MINOR carried: 3 trending links 404 on the single-fixture `TODO-OWNER`; market page lacks the mockup's "Your positions" rail section, `All/Backed` filter tabs and the compact tagged-post variant; TradingView attribution logo + outbound link: **owner 14:1x "keep the logo is fine" — DECIDED, keep the library default (`attributionLogo` untouched), which also satisfies the Apache NOTICE**; chart axis label has no thousands separator; dead chart-window buttons carry `aria-pressed`; `marketSlug()` derives from `asset.toLowerCase()` while `market.slug` is fixture data; plus carried: 390px page overflow from the pre-existing header shell, and 11px meta text in the mockup (18 places) vs CLAUDE.md's 12px rule — owner settles). **Merged into main at `7c18230`** (`--no-ff`; Claude on main: `bun install` +4 packages, check-types 3/3, web 28 / thetanuts 46 / db offline 45 tests pass, `next build` OK, secret scan clean). Worker flags: 2 new `TODO-OWNER` (chart-window buttons switch nothing; structure ordering); mockup chart end-point now ends at spot; ETH/SOL rows are example rows; no chip on text-only posts; not merged with main; needs `SKIP_ENV_VALIDATION=1` to build. Pins: `review-ui-r6.diff` SHA-256 `56166b02…9972`, 3119 lines. Opus adversarial review in flight. Original brief: Opus worker **UI round 6** in `.claude/worktrees/ui-r6` (branch `ui-r6` from `6eee798`): mockup first, then code: market page `/m/[asset]` with `lightweight-charts@5.2.1` (verified identity, pinned SHA), live structures list, the trade panel moved there; posts get three states (text-only, tagged with a market/structure chip, backed with badge + position card), Like control, no trade buttons in posts; `/t/[slug]` becomes a post thread; dead `fills`/`.acc` removed; the worker extracts fomo demo frames and reads the pump.fun screenshots for the sense; commits on `ui-r6`. Astra low **DB round 7** in `.claude/worktrees/db-r7` (branch `db-r7`): structure columns nullable as an all-or-nothing group, `tagged_asset`, backing optional ("if linked, then fenced"), `likes` table, `no_structure` AI-context reason, fixtures/tests, concurrency phase attribution, PRD v2.0 wording "optionally backed" (+ §15 note); migrations `0003`+ only (0001/0002 frozen since the push). Claude runs DB live and reviews both (Astra medium). Also in flight (wide fan-out, 11:25–11:30): Opus **core round 8** in `core-r5` (exemption pinned to addresses, decimals separated); Astra low **rewire round 2** in `.claude/worktrees/rewire2` (branch `rewire2` = main + `core-r5` + `rewire`): agent orders/sizing/tools on `@nuts/thetanuts` for BOTH sides, `THESIS_REFERRER` env default = the owner's wallet, honest per-side copy; Opus **server layer round 1** landed `15e6ca8` on `server-r1` (sign-in with wallet: EIP-4361-shaped `personal_sign`, single-use nonce via one atomic UPDATE, message rebuilt from the stored row, viem public-client `verifyMessage` for EOA/ERC-1271/ERC-6492, HMAC-signed httpOnly session, `SESSION_SECRET` from `process.env` in one module (env-package addition is a follow-up), header sign-in control that never auto-signs; reads `listFeed`/`getThread`/`getPortfolio`/`getCreator` behind `DATA_SOURCE=mock|db`; `bun test` must run from `apps/web` (server-only stub in `bunfig.toml`)); Claude: check-types OK, 82 tests, build 18 routes, `.env.local` ignored; the Astra review died on the usage limit with no verdict; Opus adversarial review (`brief-server-review-r1-opus.md`) **RED** at 12:5x (`.research/thetanuts/opus-server-review-r1.md`): 5 MAJOR — (1) in db mode `/t/[slug]` and `/u/[handle]` are SSG with empty `generateStaticParams` and no revalidate, so a page first requested while draft 404s forever and published pages never refresh; (2) `getThread`/`getCreator` list positions with no status filter, so `failed`/`pending` fills render as live participants with BaseScan links while the board excludes them; (3) a `NaN` or negative `entry_premium_usd` 500s every listing page (`BigInt()` on the raw SQL sum); (4) `reads.ts`/`map.ts` have zero tests — removing the portfolio wallet fence leaves all 82 tests green; (5) `requestSignInChallenge` is an unthrottled unbounded insert (300 rows in 395 ms) and `deleteExpiredChallenges` has no caller. 9 MINOR (header ignores wallet account switch; draft headlines join into creator participants; missing `SESSION_SECRET` only fails for returning visitors; `EXAMPLE DATA` badge hard-coded; case-variant `/u/` URLs; no server-side session revocation; `Host` header trusted for the domain line; dead chainId branch; `/new` returns null). Sound: single-use nonce under real concurrency, message-byte tests, server-action CSRF, no server code in client bundle, 12/12 auth mutations RED. Lens 7: after DB r7 the nullable structure columns break `map.ts` (`strikes.map`, `expiryAt.toISOString()`) and a comment cites the removed CHECK. Claude spot-checked M2–M5 at the bytes (true). Fold = **server round 2 (Opus writer, launched 12:46)** in the `server-r1` worktree, where Claude started `git merge main` (`dfb262a`) — five conflicted files (`src/types.ts`, `src/lib/display-types.ts`, `src/lib/display.ts`, `src/app/t/[slug]/page.tsx`, `src/app/new/page.tsx`; 65 type errors before edits) that the writer resolves on disk; Claude stages and commits. Brief `brief-server-fold-r2-opus.md`: dynamic db-mode routes (no invented revalidate), one position-status rule, NaN/negative guards, reads/map tests on its own `server_r2` database with the four reviewer mutations RED, structural challenge reuse + expiry cleanup (caps `TODO-OWNER`), MINORs, likes and nullable structure from DB r7, port 3113. Follow-ups it flagged: no `slug`/`handle` columns (routes by uuid/address), no activity writer, likes wiring after DB r7 merges, `SESSION_SECRET` into `packages/env`; Astra low **tiny-fill script** queued (`packages/thetanuts/scripts/tiny-fill.ts`: dry-run by default, `--send` only with `TINY_FILL_PRIVATE_KEY` and an owner-set `TINY_FILL_MAX_BUDGET`). Every result is re-measured by Claude, then reviewed (Astra medium), then merged. Landed so far: **rewire r2 committed `9af53cf` on `rewire2`**; Astra medium review **RED** (4 MAJOR: inherited name-based gate — fixed by core r8; raw contract units unlabelled to the model; cache returns signature-expired orders; one null feed row rejects the snapshot; 2 MINOR: binary filter advertised, stale prompt line); rewire r3 committed on `rewire2` (on main + core r8: unit-labelled amounts, signature-aware cache with deadlines, per-row feed isolation via the SDK's private `normalizeOdetteOrder` behind a documented boundary, binary copy and prompt fixed; Claude: check-types OK, 41 tests); Astra medium review r3 **RED** (3 MAJOR: string `isLong` coerced to BUY, 18-decimal-collateral call prices off by 10^12, missing SDK normalizer looks like an empty book; 1 MINOR); **rewire r4 committed `852a848` on `rewire2`** (Opus writer, 12:4x: strict zod schema on every raw row before the SDK normalizer, `buyContractSizeDecimals` = 10^6 only for 6-decimal collateral else `executable:false` "contract units unverified for this collateral" with raw base units only, `{error:"sdk_incompatible"}` surfaced verbatim by every tool, `droppedEntries` in the search tool; writer also withholds per-contract price for taker-SELL single-strike calls since `quoteSellFill` refuses them — reviewer judges). Claude: check-types 3/3, tsc clean, 49 tests, `next build` compiles; only the four `apps/web/src/lib/{agent,thetanuts}` files changed. Writer's live differential: 331 live rows, 0 rejected; blast radius of the unit gate: **62 of 124 live taker-buy orders (27 aBasWETH + 35 cbBTC single-strike calls) are non-executable until a tiny real fill proves their contract unit** — owner item. Pins: `review-rewire-r4.diff` SHA-256 `0ff424cd…18f9`, 1523 lines (`d63985c..HEAD`). Opus adversarial review r4 **RED** (12:5x, `.research/thetanuts/opus-rewire-review-r4.md`): 3 MAJOR — (1) introduced this round: `tools.ts:183` values collateral via `marketData[collateralToken.symbol]` but the map is keyed by underlying asset, so 0 of 330 live orders can ever be executable; (2) a feed payload without `orders`, or one where every row is dropped, still reads to the model as "nothing on the book" (the `?? []` fallback at `orders.ts:130`; `getMarketData` carries no `droppedEntries`); (3) `kind` is filtered after the 200-row slice so `totalMatched` is understated (multi_leg 37 of 101). 5 MINOR (sell-side 18-decimal unit asymmetry; real referrer address in `.env.example:55`; `asOf` missing on non-executable previews; `tradeableOrders` label; "Fresh snapshot" comment on a cached read). Clean: schema vs live feed 329/329 accepted, exotic numerics refused, every printed number reproduced, side copy honest, referrer forwarded, 5/5 mutations RED. Claude confirmed all three MAJORs at the bytes. Main merged into `rewire2` at `5952e61` (clean; the 7 post-merge type errors were environmental — missing `lightweight-charts` install and typed-route types — gone after `bun install` + `bunx next typegen`; tsc clean, 54 tests pass). **Rewire round 5 (Opus writer) launched 12:5x from `brief-rewire-fold-r5-opus.md`** — collateral valuation via an explicit token → price-source mapping (stablecoin peg `TODO-OWNER`), structured `feed_unusable` error, `kind` filter before the slice, MINORs; **tiny-fill script** written (`packages/thetanuts/scripts/tiny-fill.ts`; Claude: tsc OK, 5 tests, ceiling/key gates) awaiting commit after the core r8 review releases its HEAD pin; **DB r7 committed `a5634a4` on `db-r7`** (Claude live: 5 migrations, 14 tables, 54 offline, 149 integration, 16 concurrency); Astra medium review **RED** (2 MAJOR: the 0003 tag backfill forces immediate validation of every thesis and would abort an upgrade on a round-6-permitted linked draft; blank headlines allowed); round 8 committed on `db-r7` (backfill scoped with the relationship trigger suspended in-transaction; `theses_headline_nonblank` CHECK + contract trim; tests); Claude live: 58 offline, 155 integration, 16 concurrency, trigger re-enabled after migration; Astra medium review r8 **RED** (MAJOR: the headline CHECK's `[[:space:]]` class is locale-dependent, so Unicode-only whitespace could pass under a C locale — Claude's live probe on the local UTF8 database rejects NBSP, so it is a portability defect; MINORs: no commit-time validation case in the concurrency suite, `as unknown as` in the migrate fence); **round 9 committed `2dfa968` on `db-r7`** (headline CHECK = `btrim(headline, E'<25 escapes>') <> ''` over the ECMA-262 trim set, defined once in `schema/theses.ts` and copied into migration 0003 + snapshot; offline differential test `test/headline-whitespace.test.ts`; migrate fence without `as unknown as`; concurrency: publisher × mutator isolation with deferred-to-COMMIT variants). Claude live 12:2x: tsc clean; offline 64 pass / 2 skip; reset + migrate through the fence (target `127.0.0.1:54322/postgres`); full live suite **308 pass / 0 fail** across 7 files; btrim predicate agreed with `String.prototype.trim` on 11 probes incl. U+200B/U+0085/U+180E. Pins: `review-db-r9.diff` SHA-256 `c83300c9…40a0`, 8306 lines; PRD `0e18693a…4b2c`, 699 lines. Opus adversarial review r9 **GREEN** (12:5x; 64,539 code points btrim-vs-trim 0 disagreements; 18 deferred-commit schedules by hand all consistent; 28+9 fence evasions all closed; 31 mutations, all RED or provably unreachable/tsc-guarded; 2 MINOR carried to the next DB round: `?options=` is not in the fence's forbidden query-parameter set (Claude re-measured: loads with target printed; host overrides still throw; only reaches `search_path`, never the host) and README does not say `0004` has no snapshot). **DB rounds 7–9 merged into main at `239c328`** (Claude on main: check-types 3/3, web 28 / thetanuts 46 tests, live DB migrate + 308 pass, `next build` OK, secret scan clean).
- **UI** `apps/web`: rounds 1–6 GREEN and merged (round 6 at `7c18230`, see above; mockup SHA now `34bed961…566f0`); rounds 1–4 (`b88b580`): five routes on typed mock data with a domain/display split on the PRD §10.3 contract, BigInt decimal formatting, ARIA/keyboard, providers = teammate's Wagmi/Query outside + forced-dark theme inside. Round 5 (`142985d`, creator-earnings placeholders removed, porting comment fixed): Astra medium review **GREEN** (`astra-ui-review-r5.out`; two MINORs to fold next UI round: dead `fills` data chain in `types.ts`/`display-types.ts`/`display.ts`/`mock/data.ts`, dead `.acc` CSS in `index.css` and the mockup); Claude leg agrees. **Merged into main at `8c587c3`** (typecheck, 23 tests, `next build` 18 routes verified on main). Mockup SHA now `a4a191c0…`.
- **Rewire** (owner order): checkpoint `f932072` on branch `rewire` (agent `getThesisContext` tool reads `@nuts/db`). Order adapter and sizing (B/C) wait for core r6 GREEN, then both sides on `@nuts/thetanuts` with honest sell-side copy in the agent tools.
- **Revenue**: Thetanuts referrer fees (docs "Referrer Fees"): pass the platform address on every fill; fee `min(0.06% notional, 12.5% premium)`; our share `fee × splitBps / 10000` accrues per collateral token; self-claim with `claimFees(token)` FROM the referrer wallet (it must transact on Base and hold gas). The OptionBook owner must whitelist the address and set the split; unwhitelisted = 0 (seen live). Memo: `.research/thetanuts/monetisation-memo.md`. **Platform referrer wallet (owner, 2026-09-05): `THESIS_REFERRER = 0xd5E66B6d957C2d5e6C8c167707a49a029D1247dd`** (EOA on Base; measured at 11:0x +08: `referrerFeeSplitBps` = 0, i.e. not yet whitelisted; ETH balance 0, needs gas before any `claimFees`). Pass it on every quote and fill; owner still has to request the whitelist from Thetanuts. Creator payouts: roadmap only.
- **Verified market facts** (2026-09-05, decoded production fills; `.research/thetanuts/finding-fill-debits.md`, `finding-isLong-side.md`): raw `isLong` is the maker's LONG flag (true → taker SELLS, false → taker BUYS; corrected 14:4x, see the Thetanuts section); taker-buy pays `contracts × price / 1e8` (fee carved out of what the maker receives); taker-sell put posts `strike × contracts / 1e8`; RANGER seller posts inner width × contracts; contract units 1e6 for USDC-family collateral. Still UNVERIFIED: call-side and spread seller collateral beyond RANGER, non-6-decimal contract units, on-chain rounding, the fee's notional branch, signature-expiry timing (teammate: 59–113 s).
- **Writer fan-out (owner 14:2x: "split the opus into multiple ones … spin at least 2 low effort astra to code")**: five writers in parallel from `81c26ec`, each with a file fence (briefs `brief-trade-r1.md` (scope cut by message), `brief-position-r1.md`, `brief-tradecard-r1.md`, `brief-rails-r1.md`, `brief-profile-r1.md`): Opus `trade-r1` (live markets, ticket → fill → `recordTrade`, migration `0007_standalone_positions`, `getPosition` read, chart removal, post-fill share DIALOG), Opus `position-r1` (`/p/[id]` page = the P&L card hero + share image, `pnl-card.tsx` with a `compact` prop, portfolio links to `/p/`), Opus `tradecard-r1` (`/p/<uuid>` link unfurl in posts, composer `publishPost` with `?link=`/`?asset=`), Astra `rails-r1` (live feed tabs, thread spot chart removed, mock fixtures), Astra `profile-r1` (edit handle/name/bio on own profile, market-page "Your positions" rail + All/Backed tabs as unwired components). **Share card (owner 14:3x, fomo reference `.demo/fomo-share-card.png`)**: after a fill, a dialog with the P&L card (name, status chip, instrument, big signed P&L + percent, three stat tiles; numbers only — no price line), "Copy link" and "Write a post about it"; same card on `/p/[id]` and its share image. Sent to the three Opus writers by message.
- **Merged into main 15:0x–15:1x**: trade-card round 1 (`1db34bc`: `/p/<uuid>` unfurl, composer `publishPost`; verified linkage impossible under the frozen trigger → owner decision), docs round 1 (`docs/DEPLOY.md`, `bun run verify [--offline]`, env parity test, README), integration round 1 (standalone rows in portfolio/creator reads, market rail + tabs wired, production refuses `DATA_SOURCE=mock`, `metadataBase`, fence `options`). The Claude account hit its session limit again at ~15:0x: three Opus writers (core r9, position page, mockup) died mid-task and were resumed with "continue, don't redo" after the owner's `/login`. `apps/web/.env.example` now lists every schema key as an active line (parity test).
- **Core round 9 merged into main (15:2x)**: `takerSide` = `isLong ? "sell" : "buy"`, pinned to three decoded taker fills (`0x9c4bb1…` buy, `0xdf3323…` sell, and a newly found second sell `0x3e7417c5…cff04`) and a 105-fill scan (104 buy / 1 sell; 0 violations); `makerSide`, fixtures, adapter copy, READMEs and `docs/thetanuts-sdk-research.md` (dated correction block) corrected; the trade layer's `TAKER_SIDE_CONTRADICTION` gate now passes. Live after the flip: buy USDC 170 + aBasUSDC 30 executable, sell aBasUSDC 63, sell aBasWETH/cbBTC gated. **New fact:** the fee's NOTIONAL branch fires on Base — `0x3e7417…` paid fee 737 on premium 9009 (8.2%), so `feeEstimate` is an upper bound and `scripts/tiny-fill.ts` would report "Receipt mismatch" after a good fill (owner/next round). Claude verified: 50 core / 219 web / 348 DB tests, both builds; Claude's own inversion mutation 32 RED. Claude clobbered the writer's `side.ts` with `git checkout` during the probe and restored it from the transcript (rule in memory: never checkout a file in a worktree with unstaged writer edits).
- **Teammate pushed `origin/agent-exec-r1`** (15:2x, "round 1, unreviewed"): approval-gated OptionBook execution tool + approval card + wallet hand-off, built on our `buildFillTransactions` with `env.THESIS_REFERRER`; the wallet address is bound server-side as the sender. Not merged to main (their push rule ≠ merge approval). It inherits the side fix once rebased on main; include in the one-shot review when the owner says so.
- **All writer rounds merged; main `3e62739` pushed (15:4x).** Position round 1 (`/p/[id]` + `PnlCard` with `compact`, share image, portfolio links; 14 mutations, one equivalent) merged; the new fomo-feel mockup committed as the spec (`docs/mockups/thesis-fun-mockup.html` 1,417 lines + `README.md`; screenshots `.research/thetanuts/design-r1-shots/`); the six trade tests that pinned the old side contradiction rewritten as agreement tests (measured: `prepareTrade` on an unfunded wallet returns the APPROVE stage, never the side gate; `quoteFill` sizes the decoded buy fill to 389925 / 999995 by its double floor). Verified on main: check-types 3/3, thetanuts 50, DB 348, env 10, web **375** live tests, both builds, secret scan clean. Repo at this point = every feature coded (trade, positions, posts with trade cards, socials, profile, feeds, share cards, docs) on the OLD look.
- **All three UI-rebuild lanes merged; main `cc555cb` pushed (16:2x).** The market lane found a real foundation defect: `index.css` had its `:root` tokens inside `@layer components` while `packages/ui` declares shadcn's `:root` unlayered, so `--accent`/`--muted` rendered as shadcn greys on merged main; Claude confirmed in the browser and moved the token block out of the layer (accent now `#6f5cff`). Claude also measured a 390px overflow on `/m/btc` (nav Create button; 418 vs 390) → fold item 25. **UI fold round 1 (Opus HIGH) launched 16:2x** in `.claude/worktrees/ui-fold-r1` from `brief-ui-fold-r1.md` (25 items: frame unification, `--tn-*` deletion, filter pills, tab label, Follow in the rail, ONE `PnlCard` for all three card view models incl. the fill dialog via a widened `FillCard`, duplicate cards, chip copy, pages parity, dead components, label fixes, mock position fixture, mobile ticket `TODO-OWNER`, overflow). After it: the two-leg review.
- **Where things were (16:1x)**: main `32d5f12` pushed = every feature coded + two of three UI-rebuild lanes merged. **Pending:** Opus `ui-market-r1` (market page, ticket, structures, post-fill dialog, position page on the new look; 13 files on disk since 15:54, no build/screenshots yet; nudged by message at 16:1x — a queued message resumes it if it died). **Next:** merge it; then ONE UI fold round from `.research/thetanuts/brief-ui-fold-r1.md` (drafted: `PagesFrame` → `PageFrame` + `FeedRail`; delete `--tn-*` from `packages/ui`; pills become feed filters; tab "All"; Follow in the rail with follow state; one `PnlCard` for the three card view models; duplicate card on backed+linked posts; status-chip copy; pages parity; dead-component sweep; market-lane items appended after its report); then the two-leg review (lanes A–D in `.research/thetanuts/review-oneshot/`, Astra medium + Claude; ledger `review-ledger.md` items 1–24); then production migration → Vercel → tiny real fill. **Docs:** `docs/DEPLOY.md` now has the production-build smoke rule.
- **Owner items open (unchanged)**: Supabase session-pooler URL (IPv4, 5432) for the production migration; Vercel env (`DATABASE_URL` pooler 6543, `SESSION_SECRET`, `DATA_SOURCE=db`, `OPENROUTER_API_KEY`); referrer wallet whitelist + gas; the tiny real fill; product calls: verified badge on a post linking a standalone trade (schema cannot link it today), Bull/Bear naming on a post-less position, `tiny-fill.ts` fee check strict vs upper-bound (the notional fee branch fires on Base), whether to include the teammate's `origin/agent-exec-r1` in the review; every `TODO-OWNER` (presets, rankings, limits, copy).
- **UI rebuild lanes landing (16:0x)**: feed lane merged (`79e80af`: Manrope, mockup tokens + classes in `index.css`, `TopBar`/`Nav`/`FeedRail`/`PageFrame`, primitives, feed page; old shell deleted) and pages lane merged (thread, profile, composer, portfolio, wallet chip; temporary `PagesFrame` pending unification). Verified on main each time: tsc clean, 375 live web tests, both builds, and a production-build smoke in DB mode against the migrated throwaway (`DATA_SOURCE=db DATABASE_URL=…/claude_trade bunx next start`) — every route 200/404, zero server errors. **Smoke rule learned:** `next start` is production, so mock fixtures are refused by design; smoke a production build only in DB mode with an explicit migrated `DATABASE_URL` (the shared local `postgres` is behind on migrations and fails the feed query); use `next dev` for mock-mode looks. Market lane still writing. Then one UI FOLD round (the lanes' divergence lists: pills list vs filter, "Callouts" label, Follow in the rail, `/leaderboard` route, three card view models → one `PnlCard`, duplicate card on a backed+linked post, `INDEXED` chip copy, `PagesFrame` → `PageFrame`, delete `--tn-*` from `packages/ui`).
- **UI REBUILD round 1 launched 15:4x (three lanes on the new mockup)**: Opus HIGH `ui-feed-r1` (owns the FOUNDATION: Manrope, `index.css` tokens, shell = top bar + nav + persistent left feed rail + page frame, primitives; the feed page; port 3121), Opus HIGH `ui-market-r1` (market page, ticket, structures, post-fill dialog, position page; port 3122), Astra LOW `ui-pages-r1` (thread, profile, composer, portfolio, wallet chip). Look and layout only; data/actions/tests unchanged. Briefs `brief-ui-{feed,market,pages}-r1.md`; shots to `.research/thetanuts/ui-rebuild-shots/`.
- (landed) Running at 15:2x: Opus `position-r1`, `design-r1` (resumed after the session limit).
- (landed) **Running at 14:5x**: Opus `core-r9` (taker-side flip), Opus `position-r1` (`/p/[id]` + P&L card), Opus `tradecard-r1` (link unfurl + composer), Opus `design-r1` (fomo-feel mockup); Astra `integration-r1` (`brief-integration-r1.md`: standalone rows in `getPortfolio`/creator reads, wire `YourPositionsRail` + `TaggedPostsTabs` into the market page, `secret.ts`/`source.ts` on `@nuts/env`, production refuses `DATA_SOURCE=mock`, `metadataBase` from `vercelOrigin`, migrate-fence `options` param, README snapshot line); Astra `docs-r1` (`brief-docs-r1.md`: `docs/DEPLOY.md` runbook, env-example parity test, root `bun run verify` script, root README). Main is `8e9c495` (pushed).
- **Trade round 1 merged into main (14:5x)**: live markets, ticket → quote → approve/simulate/fill → `recordTrade` from chain bytes, migration `0007_standalone_positions` (chain now `0000`–`0007`; Claude regenerated the 0007 snapshot on the merged schema — the writer's pointed at 0005 — SQL byte-equal), `getPosition`, chart + library removed, post-fill share dialog. Every fill is REFUSED (`TAKER_SIDE_CONTRADICTION`) until core round 9 flips the package. Writer flags in ledger item 13 (portfolio hides standalone rows until a one-word join fix; `side` back/counter convention vs PRD §8.4 is an owner call; standalone positions can never be `creator_position_id` under the frozen trigger).
- **CORE DEFECT FOUND 14:4x (trade-r1 writer; Claude re-measured from chain bytes): `packages/thetanuts` `takerSide` is INVERTED** (`isLong ? "buy" : "sell"`; chain says `isLong=true` → taker SELLS). It passed 8 core rounds and both review legs because everyone trusted the SDK comment. Before the fix a "buy" quote on an `isLong=true` order would have produced calldata that makes the user SELL (post collateral) while the UI said "pay premium"; nothing shipped and no real fill was made. The trade layer refuses every fill (`TAKER_SIDE_CONTRADICTION`) until the package is flipped. **Core round 9 (Opus high) launched 14:5x** (`brief-core-r9.md`, worktree `core-r9`): flip `side.ts` and `markets.ts` `makerSide`, regression tests pinned to the two decoded txs, every fixture/README/adapter copy corrected, `VERIFIED_SELL_PAIRS` re-checked against `0xdf3323…` (isLong=true).
- **Launched 14:4x: Opus HIGH design round 1** in `.claude/worktrees/design-r1` (branch `design-r1` from `82efbf5`; `brief-design-r1.md`): the fomo-feel mockup (7 views + dialog + 390px feed), screenshots to `.research/thetanuts/design-r1-shots/`. The UI rebuild on it is the next writer round.
- **Rails round 1 and profile round 1 merged into main at `268507c` and pushed** (both Astra low): live Callouts (All/Following/Top) and Trending/Ending/Settled tabs with keyboard + ARIA, `src/lib/social/feeds.ts` (rules `TODO-OWNER`), thread spot chart deleted, every rail link has a mock fixture; `updateProfile` (handle/display name/bio, owner-only editor on `/u/[handle]`, `handle_taken` on 23505), session-aware "me" link; `your-positions-rail.tsx` + `tagged-posts-tabs.tsx` exist but are UNWIRED until the trade and position rounds land (exact JSX in `apps/web/src/lib/profile/REPORT.md`). Claude verified on main: check-types 3/3, web **219** live, DB 341, both builds. Remaining writers: Opus `trade-r1`, `position-r1`, `tradecard-r1`.
- (landed) **Launched 14:2x: Astra LOW rails round 1** in `.claude/worktrees/rails-r1` (branch `rails-r1` from `5b06ee5`; `brief-rails-r1.md`): live Trending/Ending/Settled and Callouts/Following/Top tabs (rules `TODO-OWNER`; `src/lib/social/feeds.ts`), thread page loses its fake spot chart (`spot-chart.tsx` deleted; payoff chart stays), every rail link gets a mock detail fixture. The Opus trade writer got the 14:1x–14:2x rulings by message (chart removal + library uninstall; migration 0007; `/p/[id]` page; trade-card unfurl of `/p/<uuid>` links in posts; composer stays text-first; no ticket-created posts).
- **Owner decisions today (14:1x–14:2x)**: remove the market-page chart (no price history at Thetanuts); allow standalone trades without a post; the logo decision is moot. Sent to the running trade writer at 14:1x (remove chart + uninstall library; migration 0007 + standalone ticket flow). Mockup `#view-market` chart block removed by Claude (spec).
- **Open owner items**: Thetanuts whitelist of the referrer wallet (given) and gas for it; a tiny real taker-BUY fill on an aBasWETH or cbBTC call to prove the non-USDC contract unit (unlocks 62 of 124 live buy orders for the agent and the market page); codex credits (out until Sep 10 15:01) or accept Opus as the interim second leg; every `TODO-OWNER` (budget presets, trending/ending rules, leaderboard formula, content limits, lock/statement timeouts); `ThesisStatus` display mapping; `/new` composer copy; single detail fixture; connected user unnamed; gap G1 (AI context for draft/pending theses).
- **Incidents today**: machine crash ~05:34 (Astra runs relaunched fresh; OrbStack needs `open -a OrbStack` after reboot); a UI worker's `pkill -f "turbo run dev -F web"` killed the owner's unrelated `naise-ai` dev server (~04:20); Claude briefs twice named non-existent things (`brief-db-writer.md`, `positions.updated_at`) — every name in a brief is verified at the bytes first.
