# Identity fold round 1 writer handoff

Uncommitted, unreviewed writer output. Zero confidence: "none of yall should trust yourselves or the other. zero confidence in everyone's responses and findings." Orchestrator must re-measure these outputs and run live checks. No writing git command, network request, database connection, migration application, build, push, deployment or real fill was performed.

## Pin and implementation

`git rev-parse HEAD`:

```text
0118a936e0ac9eb44518dc2484751616bd9e1b05
```

- `map.ts`: creators use `row.handle ?? creatorHandle(walletAddress)`; theses and positions use the stored slug. `MapPositionInput.thesis` requires `slug`. All three position/participant callers in `reads.ts` receive full `theses` rows from their existing selects, so no projection needed widening.
- `reads.ts`: `getThread(idOrSlug)` lowercases and validates the slug pattern before selecting, chooses the UUID column for UUID inputs, and uses the resolved row ID for every downstream query. `getCreator` selects wallet-shaped input by wallet, otherwise by stored handle. Old UUID/address routes still resolve. `identity.ts` retains only the documented address fallback and initials; the obsolete helper/test were removed.
- Feed, profile, participants, comments, portfolio and ranking output use these shared mappers. `page-data.ts` already forwards route parameters and uses mapped slugs/handles for share URLs and rails; no edit was needed. Mock branches and fixtures are unchanged. Domain activity now carries optional `thesisSlug` for thesis events; actor identity comes from `mapCreator`.
- Migration `0006_follow_activity` was generated by Drizzle, without hand editing: nullable `target_user_id`, users FK, `(target_user_id, created_at desc)` index, DROP/ADD of `activity_domain_reference_required` under the same name. Generated snapshot and journal accompany it. Migrations/snapshots 0000–0005 are unchanged. Actor/target relations have distinct names so the second users FK does not make relation inference ambiguous.
- `writeFollow` records the positive transition inside its existing transaction. Desired-state retries and unfollows add no event. `recordActivity` accepts target-user references. `listActivity` left-joins theses and target users, admits target-only follow rows, keeps public-thesis filtering for thesis events, and retains its confirmed-position filtering. Follow labels use the target handle/address, no thesis link or displayed money.
- README migration list, frozen range and column semantics updated. The earlier social REPORT is historical: its gaps 1 and 6 are addressed by this fold.

## Tests written

- `map.test.ts`: stored thesis slug, stored handle, lowercase address fallback, participant identities; existing position fixtures now supply slug.
- `reads.integration.test.ts`: stored slug, uppercase slug, legacy UUID, unknown slug, invalid slug with a select spy proving zero queries; handle and address lookup, null-handle fallback; resolved participant/comment reads and links.
- `schema.integration.test.ts`: target-only CHECK acceptance, all-null CHECK rejection (retained), target users FK rejection.
- `social.integration.test.ts`: follow plus desired-state retry gives exactly one target-only event; unfollow and retry leave that event unchanged; rendered follow has target handle and no transaction or thesis link; no event leaks into a thread or another actor's activity; comment activity carries its stored thesis slug.

## Measured offline outputs

`cd packages/db && DATABASE_URL='' DIRECT_DATABASE_URL='' bunx drizzle-kit generate --name follow_activity` (exit 0; explicit empty URLs prevent environment-file targets):

```text
14 tables
activity 7 columns 1 indexes 4 fks
[✓] Your SQL migration file ➜ src/migrations/0006_follow_activity.sql 🚀
```

`cd packages/db && DATABASE_URL='' DIRECT_DATABASE_URL='' bunx drizzle-kit check` (exit 0):

```text
No config path provided, using default 'drizzle.config.ts'
Reading config file '/Users/aarontan/Developer/-nuts/.claude/worktrees/identity-r1/packages/db/drizzle.config.ts'
Everything's fine 🐶🔥
```

`cd packages/db && bunx tsc --noEmit`: exit 0, no output (also run independently after the combined DB check command).

`cd packages/db && env -u DATABASE_URL SKIP_ENV_VALIDATION=1 bun test` (exit 0):

```text
schema concurrency skipped: DATABASE_URL is not set
schema integration skipped: DATABASE_URL is not set
 91 pass
 2 skip
 0 fail
 378 expect() calls
Ran 93 tests across 8 files. [1046.00ms]
```

Skipped: `packages/db/test/schema.integration.test.ts`, `packages/db/test/schema.concurrency.test.ts`.

`bun run check-types --force` (exit 0):

```text
 Tasks:    3 successful, 3 total
Cached:    0 cached, 3 total
  Time:    4.196s
```

Turbo also printed `WARNING IO error: Operation not permitted (os error 1)` cache warnings and a shell locale warning. No typecheck error was reported.

`cd apps/web && bunx tsc --noEmit`: exit 0, no output.

`cd apps/web && DATABASE_URL='' SKIP_ENV_VALIDATION=1 bun test` (exit 0):

```text
auth integration skipped: DATABASE_URL is not set
social integration skipped: DATABASE_URL is not set
reads integration skipped: DATABASE_URL is not set
 146 pass
 3 skip
 0 fail
 437 expect() calls
Ran 149 tests across 11 files. [1.78s]
```

Skipped: `apps/web/src/lib/auth/auth.integration.test.ts`, `apps/web/src/lib/data/reads.integration.test.ts`, `apps/web/src/lib/social/social.integration.test.ts`.

`git diff --check`: exit 0, no output. Added-line scan for forbidden TypeScript constructs produced no matches. No `any`, suppression directives or double assertions were introduced.

## Owner markers in changed source/docs

New: `apps/web/src/lib/data/reads.ts:426` — minimum follow label; final copy remains owner-defined.

Retained markers:

- `apps/web/src/lib/data/reads.ts:15,186,256,300,351`: existing page sizes.
- `apps/web/src/lib/data/reads.ts:87`: premium-based participation aggregation.
- `apps/web/src/lib/data/reads.ts:437`: provisional ranking formula/window.
- `apps/web/src/lib/data/map.ts:22`: existing unavailable domain metrics.
- `packages/db/src/schema/users.ts:16,18`: profile content limits; `:27`: handle bounds.
- `packages/db/README.md:105`: lock/statement timeouts; `:111`: AI draft/pending behavior; `:129`: slug prefix limits; `:144`: handle bounds.

## Gaps / orchestrator handoff

- Live migration, integration and concurrency tests are NOT validated here. Apply 0006 on an isolated database and run them before review/production gates. No `next build` attempted, as instructed (no network for fonts).
- No page/component, market/trade/thesis writer files changed. Existing expired-thesis rendering, tab controls, sign-in refresh, comment limit and pagination gaps from the social handoff are outside this fold.
- Existing activity display has no thesis-link field/rendering. Domain activity exposes `thesisSlug`, but adding clickable thesis links would require the out-of-fence `lib/display.ts` and components. Follow activity already renders through existing `socialDetail`; no new component is required. Legacy required `amountUsd: "0"` is retained internally, bypassed by social rendering and not displayed as money.
- CLAUDE.md and the review ledger were not edited because the writer path fence excludes them; orchestrator must update current state and ledger when folding.
- No reviewer verdict is claimed.

## Final path fence

 M apps/web/src/lib/data/decimal.test.ts
 M apps/web/src/lib/data/identity.ts
 M apps/web/src/lib/data/map.test.ts
 M apps/web/src/lib/data/map.ts
 M apps/web/src/lib/data/reads.integration.test.ts
 M apps/web/src/lib/data/reads.ts
 M apps/web/src/lib/social/activity.ts
 M apps/web/src/lib/social/social.integration.test.ts
 M apps/web/src/lib/social/writes.ts
 M apps/web/src/types.ts
 M packages/db/README.md
 M packages/db/src/migrations/meta/_journal.json
 M packages/db/src/schema/activity.ts
 M packages/db/src/schema/users.ts
 M packages/db/test/schema.integration.test.ts
?? apps/web/src/lib/data/REPORT.md
?? packages/db/src/migrations/0006_follow_activity.sql
?? packages/db/src/migrations/meta/0006_snapshot.json
